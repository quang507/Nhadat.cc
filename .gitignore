# Thư mục app (quá nặng, quản lý riêng)
nhadat-cc/

# KHÔNG commit — chứa credential
admin logins/

# KHÔNG commit — dữ liệu cá nhân thật (sổ đỏ, CCCD, địa chỉ)
sổ đỏ samples/

# KHÔNG commit — dữ liệu nhà + ảnh (179MB)
masterDB/

# KHÔNG commit — theme thương mại có bản quyền (274MB)
ThemeForest/

# OneDrive / OS
desktop.ini
Thumbs.db
.DS_Store

# Next.js
node_modules/
.next/
.vercel
*.tsbuildinfo

# Bridge zca-js (chạy máy local) — cookie phiên Zalo là CREDENTIAL, cấm commit
bot/bridge-zca/zalo-session.json
bot/bridge-zca/qr.png
bot/bridge-zca/package.json
bot/bridge-zca/package-lock.json
# .env chứa BRIDGE_SECRET. Repo đang PUBLIC — lọt ra là cổng FR-151 thành vô
# nghĩa. Bản mẫu không có giá trị thật thì vẫn commit (.env.example).
bot/bridge-zca/.env
!bot/bridge-zca/.env.example
# scripts/.env giữ SUPABASE_SERVICE_ROLE_KEY — khoá bỏ qua mọi RLS, lọt ra là
# mất sạch dữ liệu khách. Nguy hơn cả BRIDGE_SECRET.
scripts/.env
!scripts/.env.example

# Dữ liệu crawl & Design artifacts (để nhẹ repo, tránh tràn RAM VPS)
radanhadat-scrape/
design/
Vedoo pages/
hình samples/
SS/
*.docx
*.pptx
*.xlsx

# FR-180: kết quả train (adapter, GGUF) — nặng và sinh từ chat thật
train/out/

# vercel link kéo env về đây (chứa khoá) — chỉ ở gốc; .env.example trong scripts/ và bot/ vẫn được track
/.env.local
/.env*.local

# deno check (kiem:bot, 11/09) tu sinh lockfile — khong theo doi: CI chay deno check moi lan, khoa phien ban nam o specifier npm:.
deno.lock
