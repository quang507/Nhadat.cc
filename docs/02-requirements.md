# 02 — Requirements

Quy ước ưu tiên: **M** = Must (MVP), **S** = Should (sau MVP), **C** = Could (backlog).
Ký hiệu trạng thái: `✅` đã dựng · `🟡` một phần · `❌` chưa dựng · `⛔` deprecated.
Tài liệu này nói **yêu cầu là gì và trạng thái hôm nay**. Con trỏ hiện thực đầy đủ ở
`08-traceability.md` §8.3, kịch bản nghiệm thu ở `10-ke-hoach-kiem-thu.md`, lịch sử ở git log.

## 2.1 Mục tiêu kinh doanh

| ID | Mục tiêu | Đo bằng |
|---|---|---|
| BR-01 | Phủ 90% nguồn hàng mua/bán/thuê trên từng cụm địa bàn đang mở (Quận 5 cũ là cụm khởi điểm; địa bàn mở rộng Sài Gòn + Long An — FR-174) | Số listing active / ước lượng tổng thị trường |
| BR-02 | Xây mạng lưới 20 NMG, mỗi người chuyên một khu trong địa bàn | Số NMG active đạt chuẩn |
| BR-03 | 10 cuộc chat mới/ngày, ≥30 tin/cuộc, không rác | Bảng thống kê hội thoại (FR-71) |
| BR-04 | 1 giao dịch / 2 ngày, TB 10 tỉ | Sổ giao dịch |
| BR-05 | Thu phí bên bán: CCRB 1%, NMG 0.5% | Doanh thu ghi nhận |
| BR-06 | Miễn phí và **không thu số ĐT** của B | Chính sách sản phẩm, kiểm tra được trong log |
| BR-07 | Giữ kết nối Zalo sống suốt chu kỳ mua 3–4 năm | Tỉ lệ kết nối còn sống sau 30/90/365 ngày |
| BR-08 | SEO cho 100 keyword BĐS hot nhất | Thứ hạng Google, organic traffic |
| BR-09 | Website đẩy được B sang Zalo OA kèm ngữ cảnh | Tỉ lệ click "Bắt đầu kết nối" |

## 2.2 Persona

### P1 — Chị Dương, 42, người mua để ở
Tìm nhà Quận 5 dưới 12 tỉ, HXH. Đã bị 4 sàn gọi làm phiền nên **không để lại số ĐT**.
Hỏi rất cụ thể: sổ đỏ, hoàn công, quy hoạch, ảnh cái hẻm. Quyết định theo tháng, không theo phiên.
> Nhu cầu #1: được trả lời thật, nhanh, và không bị gọi điện.

### P2 — Anh Hưng, 35, nhà đầu tư
Quan tâm dòng tiền và khả năng kinh doanh: mở quán được không, cho thuê bao nhiêu,
xây lại được không. Xem nhiều căn, so sánh nhanh, phản hồi cụt.
> Nhu cầu #1: danh sách so sánh được, cập nhật liên tục căn mới.

### P3 — Cô Bảy, 61, CCRB
Có một căn ở Phường 4 Quận 5 cần bán. Không dùng app BĐS, chỉ dùng Zalo.
Gõ một câu là hết vốn liếng công nghệ. Sợ bị lộ thông tin, sợ mất phí oan.
> Nhu cầu #1: rao được trong 2 phút, và có người thật dẫn khách tới.

### P4 — Anh Tuấn, 29, NMG
Có 10–30 căn. Muốn thêm nguồn khách mà không tốn phí đăng tin.
Chấp nhận trả 0.5% khi thành công, nhưng phải nhận được câu hỏi của khách **ngay**.
> Nhu cầu #1: dòng lead ổn định, phản hồi câu hỏi nhanh để không mất khách.

### P5 — Ngân, 26, CTV nhadat.cc
1.5 FTE cho toàn hệ thống. Nhận lịch xem nhà, gọi CCRB, dẫn khách, chấm điểm NMG.
> Nhu cầu #1: một hàng đợi công việc rõ ràng, có ưu tiên.

## 2.3 Phạm vi

### Trong phạm vi MVP
Website nhadat.cc (giới thiệu, listing, search, SEO, CTA Zalo) · Zalo OA chatbot cho B ·
Mini-site rao tin cho S · Vòng hỏi-đáp bổ sung thông tin S↔B · Đặt lịch xem nhà ·
Admin backend buyer side · Email notification · Bảng thống kê xuất Excel.

### Ngoài phạm vi MVP
App native · Thanh toán online · Ký hợp đồng điện tử · Định giá tự động đầy đủ ·
Facebook Messenger / Telegram · Đa ngôn ngữ · Cổng NMG tự phục vụ đầy đủ
(MVP dùng CTV vận hành thủ công).

---

## 2.4 Yêu cầu chức năng

### A. Website công khai (WEB)

| ID | Yêu cầu | Ưu tiên | Nguồn |
|---|---|---|---|
| FR-01 | ✅ **Landing giới thiệu dịch vụ.** Mỗi mục một lợi ích kèm minh hoạ hội thoại, theo phong cách whatsapp.com. Hiện thực: `app/page.tsx` khối "Lời hứa". | M | website.docx §Giới thiệu dịch vụ |
| FR-02 | ✅ **Ô search dạng chat ở trang chủ.** Placeholder là một câu nói tự nhiên; gõ xong đi thẳng vào kết quả. Hiện thực: form GET ở hero → `app/api/search`. | M | website.docx §Search |
| FR-03 | ✅ **Khối "Hỏi bất kỳ, có tức thì".** Minh hoạ vòng B hỏi → hệ thống hỏi S → báo lại B. Hiện thực: `app/page.tsx` khối 3 bước. | M | website.docx |
| FR-04 | ✅ **Khối cam kết riêng tư.** Ba vế: không hỏi số ĐT, chỉ liên hệ qua Zalo, ngắt kết nối bất cứ lúc nào. Hiện thực: `app/page.tsx`, nhắc lại ở hộp Zalo trang kết quả. | M | website.docx §Tụi em không hỏi số ĐT |
| FR-05 | 🟡 **Cam kết theo dõi dài hạn.** Khối "Mất vài năm mới tìm được nhà?" hứa đi cùng khách suốt chu kỳ mua. Hiện thực: thẻ "Trực 24/7, nhớ nhu cầu" ở `app/page.tsx`. Chưa: khối riêng đúng thông điệp gốc. | M | website.docx |
| FR-06 | ✅ **CTA sang mini-site người bán.** "Bán hay cho thuê với nhadatCC?" dẫn sang `/raoban`. Hiện thực: `app/page.tsx` khối navy. | M | website.docx |
| FR-07 | ✅ **Trang listing dạng lưới card.** Mỗi card: ảnh, mô tả rút gọn, vị trí, DT, giá. Hiện thực: `ListingBrowse`, `/mua-ban`, `/cho-thue`. | M | website.docx §Listing |
| FR-08 | ✅ **Trang kết quả nói lại truy vấn.** "Tìm thấy N tin theo yêu cầu" + tiêu đề diễn giải lại câu người dùng gõ; hộp Zalo mang theo câu đó. Hiện thực: `ListingBrowse` nhận `q`. | M | website.docx §Search |
| FR-09 | 🟡 **Search hiểu ngôn ngữ tự nhiên.** Bóc loại BĐS, quận/phường/đường/mốc, giá, HXH/MT, số PN, mua hay thuê; có dấu lẫn không dấu. Hiện thực: `lib/parse-query.ts`, `app/api/search`. Chưa: tự nới kết quả khi không khớp (thay bằng chip lọc + Zalo). | M | website.docx §Search; INS-07 |
| FR-10 | ✅ **Trang chi tiết BĐS.** Gallery ảnh, mô tả đầy đủ, bảng thông số, bản đồ vị trí ở mức phường (FR-104). Hiện thực: `app/nha-dat/[code]`, `components/WardMap`. | M | website.docx §Chi tiết 1 BĐS |
| FR-11 | ✅ **Cue nhắc mã tin.** Trang chi tiết luôn gợi "khi nhắn Zalo nhớ hỏi #mã". Hiện thực: `app/nha-dat/[code]` hộp Zalo + câu xin hình theo mã. | M | website.docx §Cue |
| FR-12 | 🟡 **Trang tag SEO.** 100 tag BĐS hot nhất, mỗi tag một URL tĩnh: H1 = keyword, mô tả sinh từ số liệu kho, lưới tin khớp, tag liên quan; trang rỗng thì mời Zalo chứ không 404. Hiện thực: `lib/tags.ts`, `app/[tag]` (SSG, 64 tag). Chưa: bộ TOP-100 keyword thật (OPEN-06) và tag theo khu mới (OPEN-27). | M | website.docx §Tag |
| FR-13 | ✅ **Hộp mời kết nối Zalo.** Hiện ở trang chủ và mọi trang kết quả tìm kiếm. Hiện thực: `ListingBrowse`, `app/page.tsx`. | M | website.docx §Hộp mời kết nối |
| FR-14 | ✅ **Chuyển sang Zalo mang theo ngữ cảnh.** Click "Bắt đầu kết nối" thì toàn bộ ngữ cảnh tìm kiếm của phiên đi cùng. Hiện thực: `zaloLink()`, tham số `search:<q>` / `#mã`. | M | website.docx §Hộp mời kết nối |
| FR-15 | 🟡 **Điều hướng nội bộ trước khi sang Zalo.** Khuyến khích B xem 3–5 trang rồi mới click sang Zalo. Hiện thực: breadcrumb, khối "Cùng khu", chip phường, tag liên quan, link tính lãi vay. Chưa: chưa có số đo số trang mỗi phiên. | S | website.docx §Tăng cường SEO |
| FR-16 | ❌ **Fingerprint khách vãng lai.** Ghi nhận hành vi để cá nhân hoá kết quả lần sau. Chưa: thư viện, chính sách lưu trữ và cơ chế từ chối còn treo (OPEN-14). | S | PDF hệ thống §1 |
| FR-17 | ✅ **Nền SEO kỹ thuật.** Sitemap.xml, robots, meta/OG và canonical tự sinh, JSON-LD `RealEstateListing` trên trang tin (địa chỉ tới mức phường — FR-104). Hiện thực: `app/sitemap.ts`, `app/robots.ts`, `app/layout.tsx`. | M | BR-08 [giả định BA] |

### B. Zalo OA — phía mua (BOT)

| ID | Yêu cầu | Ưu tiên | Nguồn |
|---|---|---|---|
| FR-20 | ✅ **Chào lần đầu.** Giới thiệu "30 nhà môi giới túc trực", xin tên để xưng hô. Hiện thực: `chat-reply` + `bot_prompts`. | M | chats w B.docx §Chào hỏi |
| FR-21 | ✅ **Chào khi gặp lại.** Nhắc lại tiêu chí lần trước và hỏi xác nhận còn đúng không. Hiện thực: `chat-reply` (hồ sơ `buyers.preferences`). | M | chats w B.docx §Nếu gặp lại |
| FR-22 | 🟡 **Truy vấn theo vị trí ở mọi mức.** Quận, phường, đường, số hẻm, ngã tư, mốc tiện ích, hoặc "gần căn này". Hiện thực: `chat-reply` lọc KHO theo phường/quận. Chưa: luật riêng cho mốc tiện ích và "gần căn này". | M | chats w B.docx §Hỏi nhà quanh 1 vị trí |
| FR-23 | 🟡 **Truy vấn giá.** Khoảng, dưới, trên, giữa hai mốc, hoặc "giá cỡ căn này quanh đây". Hiện thực: `budgetRangeVnd` trong `chat-reply`. Chưa: neo giá theo một căn mốc khi khách hỏi. | M | chats w B.docx §Hỏi giá |
| FR-24 | 🟡 **Tối đa 3 listing mỗi tin nhắn**, kèm nút "Xem thêm". Hiện thực: trần 3 căn là bất biến trong prompt `chat-reply`, mời xem thêm bằng lời. Chưa: nút bấm — Zalo cá nhân không có, chờ OA (OPEN-43). | M | chats w B.docx; S's side.docx §Show a list |
| FR-25 | ✅ **Định dạng listing trong chat**: `#mã – mô tả 1 dòng – giá`. Hiện thực: khối KHO của `chat-reply`. | M | chats w B.docx |
| FR-26 | ✅ **Khai thác nhu cầu sâu.** Hỏi mua ở hay đầu tư; nếu kinh doanh thì hỏi loại hình. Hiện thực: `chat-reply` (nhịp hỏi FR-130). | M | chats w B.docx §Lấy thêm nhu cầu |
| FR-27 | ✅ **Gửi ảnh theo yêu cầu.** Mỗi lượt vài tấm rồi hỏi "xem thêm không"; nhớ chỗ đang dừng để lượt sau gửi tiếp. Hiện thực: `chat-reply` (≤4 tấm/lượt, offset trong `buyers.preferences`). Chưa: mỗi khách chỉ nhớ một offset nên xin hình căn khác giữa chừng là mất chỗ cũ. | M | chats w B.docx §Khi chỉ gửi 1 số hình |
| FR-28 | ❌ **Tiện ích quanh BĐS.** Trả lời trường học, chợ, bệnh viện kèm khoảng cách. Chưa: nguồn dữ liệu POI còn treo (OPEN-13). | S | chats w B.docx §Khi trả lời về tiện ích |
| FR-29 | ✅ **Vào từ quảng cáo với một mã BĐS.** Chào đúng căn đó ngay. Hiện thực: khối "căn khách đang nhắc" trong `chat-reply`. | M | chats w B.docx §Hỏi đáp từ quảng cáo |
| FR-30 | ✅ **Nhận ngữ cảnh tìm kiếm từ website.** Câu tìm kiếm đi kèm khi B click hộp mời kết nối. Hiện thực: `zaloLink()` → `chat-reply`. | M | website.docx; FR-14 |
| FR-31 | ✅ **Gợi ý căn tương tự.** Căn khách hỏi đã chốt/đã gỡ, hoặc khách nói "giống giống vầy" → tối đa 3 căn cùng deal, giá 0,7–1,3× căn gốc, ưu tiên cùng phường rồi mở ra cùng quận. Hiện thực: khối CĂN TƯƠNG TỰ trong `chat-reply`. | S | chats w B.docx |
| FR-32 | ✅ **Chủ động khi B im sau một BĐS.** Gửi thêm thông tin mà không cần được hỏi. Hiện thực: `tao_followup` + `nudge` (một nhắc/khách/24h). | S | chats w B.docx §Khi khách không hỏi gì thêm |

### C. Vòng hỏi-đáp bổ sung thông tin (ASK)

| ID | Yêu cầu | Ưu tiên | Nguồn |
|---|---|---|---|
| FR-40 | ✅ **Không chắc thì không đoán.** AI cần xác minh thì tạo một yêu cầu thông tin gửi phía bán thay vì phán bừa. Hiện thực: trường `ask_owner` → `info_requests`. | M | chats w B.docx; RSK-03 |
| FR-41 | ✅ **Yêu cầu mang đủ ngữ cảnh**: mã BĐS, mã yêu cầu, nguyên văn câu hỏi của B. Hiện thực: `info_requests` (một CSDL chung thay cho hai API rời). | M | chats w B.docx §Gọi API về S side |
| FR-42 | ✅ **Định tuyến câu hỏi.** Về chủ tin (CCRB/NMG), hoặc CTV, hoặc admin. Hiện thực: trigger `route_info_request` (câu khách hỏi đi CTV — FR-173; câu drip đi chủ nhà). | M | chats w B.docx |
| FR-43 | ✅ **Câu trả lời quay về kèm nguyên văn và file/ảnh.** Hiện thực: nhánh nội bộ `#mã: trả lời` và nhánh người bán của `chat-reply` → `listing_facts`. | M | chats w B.docx |
| FR-44 | ✅ **Trả lời của S cập nhật luôn listing**, phục vụ mọi B sau đó. Hiện thực: `ghi_fact_listing` → `listing_facts_sync_cols` → cột `listings`. | M | S's side.docx §Hỏi thêm thông tin từ S |
| FR-45 | ✅ **Giữ nhịp trong lúc chờ S.** Kết tin bằng "trong khi chờ, anh/chị có câu hỏi gì khác không ạ?". Hiện thực: luật + few-shot trong `bot_prompts`. | M | S's side.docx |
| FR-46 | ✅ **Nhóm câu hỏi chuẩn hoá**: còn bán không · sổ đỏ · quy hoạch · kinh doanh được không · ảnh hẻm/khu vực · hoàn công. Hiện thực: `required_facts`, view `listing_missing_facts`. | M | chats w B.docx |
| FR-47 | ✅ **SLA cho yêu cầu thông tin.** Quá hạn thì leo thang sang CTV rồi admin. Hiện thực: `info_request_sla_tick` (120 phút CTV — FR-173) và `info_request_timeout_tick` (24h/48h — FR-110). | S | [giả định BA] |

### D. Đặt lịch xem nhà (VIEW)

| ID | Yêu cầu | Ưu tiên | Nguồn |
|---|---|---|---|
| FR-50 | ✅ **Khởi động đặt lịch.** B đề nghị xem nhà, hoặc bot chủ động đề nghị khi thấy B quan tâm. Hiện thực: trường `viewing` trong `chat-reply`. | M | chats w B.docx §Hẹn xem nhà |
| FR-51 | ✅ **Xác nhận đúng căn trước khi chốt giờ** (mã, địa chỉ, giá). Hiện thực: `chat-reply` (mã tin viết HOA, neo `listing_id`). | M | chats w B.docx |
| FR-52 | ✅ **Chốt lịch qua CTV.** Thu khung giờ mong muốn, nhắc CTV ngay để xác nhận với chủ nhà trong 120 phút; CTV nhắn `#mã: ok 9h` thì lịch sang `confirmed` và bot báo khách. Quá hạn thì leo thang admin. Hiện thực: trigger `viewings_bao_ctv_va_email`, `nudge`. | M | chats w B.docx |
| FR-53 | ✅ **Xin số điện thoại chỉ tại bước này**, nêu rõ mục đích, cho phép từ chối. Hiện thực: `chat-reply` nhánh `viewing`. | M | chats w B.docx; xung đột INS-04 → OPEN-05 |
| FR-54 | ✅ **Gửi link Google Maps ghim vị trí.** Chỉ gắn khi tin có toạ độ thật, không có thì không bịa link. Hiện thực: `nudge` nhắc `viewing` + `listings.lat/lng` (FR-122). | M | chats w B.docx |
| FR-55 | ✅ **Nhắc trước giờ hẹn.** CTV gọi trước 30 phút; bot nhắn trước buổi xem 45 phút, kèm link bản đồ khi tin có toạ độ. Hiện thực: `reminders` kind `viewing` + `nudge`. | M | chats w B.docx §Nhắc trước khi xem |
| FR-56 | 🟡 **Hỏi cảm nhận sau khi xem.** Hỏi sau buổi xem 4 giờ, mỗi buổi một câu; không ưng thì hỏi lý do để tinh chỉnh tiêu chí. Hiện thực: `reminders` kind `feedback` + `nudge` (mẫu cố định). Chưa: nhánh riêng đọc lý do chê để sửa hồ sơ nhu cầu. | M | chats w B.docx §Không chịu xem nhà |
| FR-57 | 🟡 **Yêu cầu xem nhà sinh sự kiện + email `[VIEWING]` cho admin.** Hiện thực: trigger `viewings` → `property_events` + `email_admin`. Chưa: email chưa đi thật vì ntfy chặn gửi ẩn danh (xem FR-81). | M | chats w B.docx §Hẹn xem nhà (backend) |

### E. Giữ chân & tái kích hoạt (RET)

| ID | Yêu cầu | Ưu tiên | Nguồn |
|---|---|---|---|
| FR-60 | ✅ **Follow-up sau 5 ngày im lặng.** Một lần cho mỗi lượt im, cửa sổ tới 7 ngày, chỉ gửi trong khung 8–21h VN. Hiện thực: `nudge` + `buyers.last_contact_at`. | M | chats w B.docx §Follow-up; quyết định chủ dự án 04/09/2026 |
| FR-61 | ✅ **Nội dung hỏi thăm đa dạng.** Sáu góc xoay vòng tất định: căn cuối, tiến độ, xem ảnh, tiêu chí, đặt lịch, thị trường; góc cần căn mà không có căn thì nhảy góc kế. Hiện thực: `nudge`. | M | chats w B.docx §Hỏi về căn nhà cuối cùng |
| FR-62 | ✅ **Chào căn khác cùng khu / cùng tầm giá** để khởi động lại hội thoại. 2–3 căn cùng phường (hoặc cùng quận), giá 0,7–1,15× căn mốc, trừ căn đã gửi. Hiện thực: hàm `can_cung_khu()` + `nudge`. | M | chats w B.docx §Chào những căn khác |
| FR-63 | ✅ **Chống Zalo xoá kết nối.** Im ≥6 ngày (trước mốc 7 ngày) thì buộc gửi câu xin nhắn lại một chữ, bằng mẫu cố định nên key model hỏng vẫn cứu được kết nối. Hiện thực: `nudge` góc `giu_ket_noi`. | M | chats w B.docx §Trước khi Zalo xóa; RSK-01 |
| FR-64 | ✅ **Báo tin mới khớp tiêu chí đã lưu.** Tin vừa lên kệ khớp deal + khu vực + ngân sách (0,7–1,15× giá tin) của khách còn liên hệ trong 30 ngày → báo chủ động. Van: 1 tin/khách/24h, 1 tin/khách/tin, tối đa 50 khách/tin. Hiện thực: `bao_tin_moi_khop()` + `reminders` kind `match`. | M | website.docx §Mất vài năm mới tìm được căn nhà |
| FR-65 | 🟡 **Xin đánh giá chất lượng ở 3 thời điểm**: sau khi cấp thông tin một BĐS, sau một đợt tìm kiếm, sau khi xem nhà. Hiện thực: sau buổi xem — khách chấm 1–5 sao trong 48h → `ghi_danh_gia()`. Chưa: hai thời điểm còn lại (cố ý hoãn để khỏi spam). | S | chats w B.docx §Đánh giá chất lượng |

### F. Admin — buyer side (ADM)

| ID | Yêu cầu | Ưu tiên | Nguồn |
|---|---|---|---|
| FR-70 | 🟡 **Bảng sự kiện BĐS**: timestamp, mã BĐS, loại sự kiện; chỉ cần kết nối được từ Excel. Hiện thực: bảng `property_events` sinh bằng trigger từ 7 bảng nguồn, thẻ "BĐS hot" ở `/admin`, đọc/xuất qua Table Editor. Chưa: xuất Excel phía server; loại `photos` chưa có nguồn sinh. | M | chats w B.docx §Backend của B Side |
| FR-71 | 🟡 **Bảng thống kê hội thoại**: mã B, timestamp, số tin mỗi bên, thời lượng. Hiện thực: view `hoi_thoai_thong_ke` theo ngày + thẻ 30 ngày và nút tải CSV ở `/admin`; số liệu theo từng hội thoại nằm ở view `hoi_thoai_phien` (FR-72). Chưa: thẻ theo từng hội thoại trên `/admin`, xuất Excel phía server. | M | chats w B.docx §Thống kê cuộc trò chuyện |
| FR-72 | ✅ **Định nghĩa cuộc trò chuyện**: chuỗi tin cách nhau không quá 30 phút. Hiện thực: view `hoi_thoai_phien` tách phiên từ `messages`; model `conversations` giữ nguyên một hội thoại/khách. | M | chats w B.docx |
| FR-73 | ✅ **BĐS hot nhất** = đếm sự kiện trong 2 tháng gần nhất. Hiện thực: view `bds_hot` (tính lúc đọc) + thẻ "BĐS hot · 60 ngày" ở `/admin`. | M | chats w B.docx §BĐS 'hot' nhất |
| FR-74 | 🟡 **Tìm kiếm B.** Lọc theo tên Zalo, theo khoảng thời gian tiếp xúc lần đầu/lần cuối/một cuộc trò chuyện. Hiện thực: ô "Tìm khách" ở `/admin` (tên hoặc Zalo uid), không hiện `phone` (NFR-07). Chưa: lọc theo khoảng thời gian. | M | chats w B.docx §Tìm kiếm B |
| FR-75 | 🟡 **Nhảy từ kết quả sang Zalo xem lịch sử hội thoại.** Hiện thực: link `zalo.me/<uid>` khi có uid. Chưa: chưa có bằng chứng uid mở được trên Zalo thật (best-effort). | M | chats w B.docx |
| FR-76 | 🟡 **Danh sách câu hỏi của B chờ S trả lời**: mã B, mã BĐS, giờ hỏi, câu hỏi, giờ và nội dung trả lời. Hiện thực: thẻ "Câu hỏi đang chờ" ở `/admin` (kèm người được giao, hạn SLA đỏ khi quá hạn). Chưa: mã B bấm được sang Zalo từ dòng câu hỏi. | M | chats w B.docx §Các câu hỏi của B cần S trả lời |
| FR-77 | 🟡 **Danh sách phản ứng tiêu cực**: giờ, mã B, trích nguyên văn vài tin. Hiện thực: view `khach_can_nguoi_that` + thẻ ở `/admin`, dùng cờ `needs_human` làm proxy. Chưa: bot chưa gán nhãn "tiêu cực" riêng. | M | chats w B.docx §Phản ứng tiêu cực |
| FR-78 | ✅ **Danh sách hẹn xem nhà**: giờ, mã B, mã BĐS. Hiện thực: thẻ "Lịch xem nhà" ở `/admin` (sắp tới + 7 ngày qua). | M | chats w B.docx §Hẹn xem nhà |
| FR-79 | 🟡 **Danh sách yêu cầu voice chat.** Khách đòi gọi điện → bật cờ cần người thật, sinh việc `VOICE:` và email `[VOICE]`; bot hứa nhờ người phụ trách gọi lại. Hiện thực: `chat-reply` + `reminders`. Chưa: thẻ riêng trên `/admin` (nay đọc chung bảng `reminders`). | M | chats w B.docx §Khách hàng muốn voice chat |
| FR-80 | ✅ **Mọi danh sách admin phân trang 20 mục/trang.** Hiện thực: `usePhanTrang` ở `/admin`, dữ liệu tải một đợt nên lật trang không tốn truy vấn. | M | chats w B.docx |
| FR-81 | 🟡 **Email cho admin ở mọi mục FR-76…79.** Tiêu đề `<loại> <Zalo ID>`, loại ∈ `[QUESTION] [VOICE] [VIEWING] [UPSET]`, thân thư chứa các trường + mô tả BĐS. Hiện thực: `email_admin()` qua ntfy.sh, đích đọc từ `app_config.admin_email`. Chưa: ntfy chặn gửi email ẩn danh — chờ chủ dự án tạo tài khoản + `NTFY_TOKEN`. | M | chats w B.docx §Email notification |

### G. Mini-site & luồng người bán (SEL)

| ID | Yêu cầu | Ưu tiên | Nguồn |
|---|---|---|---|
| FR-90 | ⛔ [deprecated → FR-158] **Mini-site rao bán/cho thuê 2024.** Thay thế hoàn toàn bằng câu rao tự nhiên trong Zalo (FR-158) không bắt điền form. | — | S's side.docx; chốt 08/09/2026 OPEN-48 |
| FR-91 | ✅ **Rao bằng một câu thông thường**, không bắt điền form nhiều trường. Hiện thực: `/raoban` → Zalo, nhánh câu rao của `chat-reply`. | M | S's side.docx; INS-05 |
| FR-92 | ✅ **AI bóc tách trường từ câu rao**: vị trí, đường/loại hẻm, quy mô, giá, mua/thuê. Hiện thực: `app/api/listing/parse` (xem trước) + `boc_thong_so()` phía DB (FR-172). | M | S's side.docx §Bóc tách trường |
| FR-93 | ❌ **Sinh nhiều biến thể câu rao** theo độ dài và theo khía cạnh B quan tâm. Chưa: chưa dựng ở cả web lẫn bot. | M | S's side.docx |
| FR-94 | ⛔ [deprecated → FR-177] **Màn xác nhận bóc tách trên web 2024.** Thay bằng gửi bản nháp tin và gật nhận trực tiếp ngay trong Zalo chat (FR-177). | — | PDF hệ thống §2; chốt 08/09/2026 OPEN-48 |
| FR-95 | ⛔ [deprecated → Bridge Zalo] **Đăng nhập Zalo SSO 2024.** Không dùng tài khoản web, người dùng định danh tự nhiên qua zalo_user_id. | — | chốt 08/09/2026 OPEN-48 |
| FR-96 | ✅ **Upload nhiều ảnh cho listing.** Nén ở trình duyệt rồi đẩy vào bucket công khai theo UUID tin và ghi `listing_media`; ảnh sổ đỏ không đi đường này (NFR-06). Hiện thực: `components/UploadAnh` ở `/admin`, `/admin/dang-tin`, `/quan-ly`. | M | PDF hệ thống §3 |
| FR-97 | ⛔ [deprecated → FR-109] **Link mini-site cho người muốn bán.** Bot gửi link `/raoban` khi có người nhắn Zalo OA muốn bán; nay rao từng bước ngay trong Zalo. | — | S's side.docx §List new |
| FR-98 | 🟡 **S trả lời câu hỏi bổ sung ngay trong chat**, kể cả gửi ảnh và giấy tờ. Hiện thực: nhánh người bán của `chat-reply`; ảnh (kèm hay không kèm chú thích) đi FR-185: phân loại, tải về kho, `listing_media` (OPEN-32 đóng 09/09/2026). | M | S's side.docx §Hỏi thêm thông tin từ S |
| FR-99 | 🟡 **Định giá bằng so sánh nhanh.** Bot và trang tin đối chiếu giá/m² trung bình cùng phường + cùng loại giao dịch, nói rõ là ước tính từ kho và là giá rao. Hiện thực: khối KHO của `chat-reply`, dòng so sánh ở `app/nha-dat/[code]`. Chưa: hỗ trợ phía người bán lúc rao, nguồn giá thị trường ngoài (OPEN-10). | S | S's side.docx §Dịch vụ của nhadatCC cho S |
| FR-100 | ⛔ [deprecated → CRM /admin] **Danh sách riêng cho một khách.** Không nằm trong luồng Aioinhadat; thay bằng bảng CRM khách hàng trên /admin. | — | chốt 08/09/2026 OPEN-48 |
| FR-101 | ✅ **Phân loại S là CCRB hay NMG để áp đúng mức phí.** Nhãn gán ngay lúc bóc tách: ai nói mình có BĐS là chính chủ, chỉ người tự xưng môi giới mới là NMG. Hiện thực: `mo_ho_so_nguoi_ban()` (FR-159). | M | biz model.docx |
| FR-102 | ⛔ [deprecated → FR-137] **Theo dõi chuẩn NMG.** Ràng buộc chất lượng NMG (≥10 listing, tỉ lệ thành công ≥5%, rating >3/5); việc chấm điểm nay nằm trọn trong báo cáo CTV 17h. | S | biz model.docx §NMG; chủ dự án chốt 27/08/2026 |
| FR-103 | ✅ **Lời hứa "rao một lần là xong".** Hệ thống theo đuổi việc bán tới khi gặp người mua phù hợp, chỉ liên hệ lại S khi cần xác minh hoặc chốt lịch; tin `dang_ban` im 30 ngày thì hỏi lại "còn bán không" (một lần mỗi 30 ngày, trần 5 tin/ngày). Hiện thực: `stale_listing_tick()` + cron 9h VN. | M | trao đổi chủ dự án 22/08/2026; INS-09 |

### H. Trung gian ẩn danh & vòng đời listing (BROKER)

Nguồn cả nhóm: artifact "Cầu Nối BĐS" v2 (08/2026) — spec này thắng khi mâu thuẫn với mục cũ.

| ID | Yêu cầu | Ưu tiên | Nguồn |
|---|---|---|---|
| FR-104 | ✅ **Không chủ động phơi thông tin, khai khi được hỏi.** Web và gợi ý của bot chỉ nêu mã tin + khu vực mức phường; khách hỏi một căn cụ thể thì bot khai mọi thứ đã lưu về căn đó; SĐT/Zalo người bán chỉ mở ở bước hẹn xem; hai bên không nhắn trực tiếp cho nhau. Hiện thực: `sanitizeDescription()` phía web, khối căn-đang-nhắc của `chat-reply`. | M | INS-11; quyết định chủ dự án 02/09/2026 |
| FR-105 | 🟡 **Lọc liên hệ trước khi chuyển tiếp.** Mọi fact và mọi bong bóng gửi người mua bị soát SĐT / Zalo / địa chỉ số nhà, thay bằng "[liên hệ qua Zalo]"; không áp cho nhánh người bán / CTV / admin. Hiện thực: `locLienHe()` (bot) và `sanitizeDescription()` (web) — hai bản phải sửa cùng nhau. Chưa: duyệt ảnh tay giai đoạn đầu. | M | INS-11 |
| FR-106 | ⛔ [deprecated → FR-139] **Vòng đời listing bản cũ.** Chuỗi trạng thái tiếng Anh (draft → pending_review → active → negotiating → sold/expired); nay 5 trạng thái tiếng Việt và không còn bước duyệt tay bắt buộc. | M | Cầu Nối §Vòng đời |
| FR-107 | ⛔ [deprecated → FR-129/FR-144] **TTL xác nhận 7 ngày.** Xác nhận lại với S bằng `last_confirmed_at` trước khi giới thiệu; nay việc giữ tin tươi do vòng hỏi nhỏ giọt lo, riêng TTL 7 ngày cho căn dự án còn sống trong FR-116. | M → S | Cầu Nối §F2; DH-03 |
| FR-108 | ✅ **Sổ quan tâm và báo khi căn đã chốt.** Mọi căn khách nhắc/bot gợi/xin hình đều ghi `interests`; tin sang `da_chot` thì báo mọi B đang chờ kèm 1–2 căn thay thế cùng khu. Van: 1 tin/khách/24h và 1 tin/khách/tin. Hiện thực: `mark_listing_interest`, `bao_can_da_chot()`, `nudge` mẫu cố định. | M | Cầu Nối §F2 |
| FR-109 | ✅ **Rao tin từng bước ngay trong Zalo**: khu vực → loại → giá → DT → pháp lý → mô tả → ảnh; khu vực không khớp danh mục thì đưa lựa chọn quận/phường. `/raoban` là kênh song song trên web. Hiện thực: nhánh câu rao + drip của `chat-reply`. | M | Cầu Nối §F1; thay FR-97 |
| FR-110 | ✅ **Timeout hỏi S.** Nhắc 1 lần sau 24h; quá 48h đóng yêu cầu và báo trung thực cho B kèm gợi ý căn khác. Hiện thực: `info_request_timeout_tick()` + cron mỗi giờ, `followup` → `nudge`. | M | Cầu Nối §F3 |
| FR-111 | 🟡 **Ảnh từ Zalo là URL tạm.** Phải tải về kho file, DB chỉ lưu tham chiếu. Hiện thực: kho Supabase Storage + `listing_media` (FR-165); adapter đổi kho cố ý không dựng, `app_config` đủ để trỏ URL đi nơi khác. Ảnh chủ gửi qua chat tải về kho từ 09/09/2026 (FR-185; OPEN-32 đóng). | M | Cầu Nối §Kiến trúc |
| FR-112 | ✅ **Sổ giao dịch làm căn cứ tính phí.** Điểm sao từng tương tác ghi vào hồ sơ; `deals` ghi giao dịch thành công với phí CCRB 1% (CTV hưởng 0.5%) và NMG 0.5%, kèm tỉ lệ chốt 5% của NMG. Hiện thực: bảng `deals` + `fee_pct` trong `chat-reply`. | M | Cầu Nối §F4; OPEN-16 (b) |

### I. Hàng dự án (PROJECT)

Nguồn cả nhóm: OPEN-15 phương án (b), chủ dự án 24/08/2026; phân tích INS-10.
Phạm vi MVP: **đặt nền dữ liệu**, chưa làm UI giỏ hàng riêng.

| ID | Yêu cầu | Ưu tiên | Nguồn |
|---|---|---|---|
| FR-113 | ✅ **Data model dự án.** Bảng `projects` (tên, slug, chủ đầu tư, vị trí, pháp lý, tiện ích, mặt bằng, tiến độ) + liên kết trên listing: `project_id`, `unit_code`, `floor`, `direction`, `unit_status` ∈ {còn bán, giữ chỗ, đã cọc, đã bán}. Hiện thực: SRS-3.10. | M | OPEN-15 (b); INS-10 |
| FR-114 | 🟡 **Gắn tin vào dự án lúc rao.** Chọn dự án có sẵn + nhập mã căn; không thuộc dự án thì tin là hàng lẻ. Hiện thực: luồng Zalo (`match_projects` → `project_id` + `unit_code`). **(d) Bot tự lôi kiến thức dự án ra khi chủ nhà nhắc** [chủ dự án 09/09/2026 chiều (đối chiếu chat Gemini "AI Ơi Nhà Đất" 21–27/06/2026, chốt từng dòng OPEN-55); chat 21/06 "Sunrise City theo em biết có hồ bơi Olympic"]: nhánh bán gọi `match_projects` khi câu có dấu hiệu tên dự án, nạp khối "DỰ ÁN (kiến thức ĐÃ XÁC THỰC trong kho)" (tên, CĐT, tiện ích, mô tả) vào ngữ cảnh model, dặn khen bằng đúng MỘT tiện ích có trong khối, không bịa; căn đang hỏi chưa gắn dự án thì gắn `project_id`. Kho = bảng `projects` (admin nhập) — không có thì bot không biết. Chưa: chọn dự án ở form admin. | M | OPEN-15 (b); chủ dự án 09/09/2026 |
| FR-115 | ✅ **Trả lời câu hỏi tầng dự án từ dữ liệu chung** (vị trí, chủ đầu tư, pháp lý, tiện ích, tiến độ), KHÔNG tạo info_request — vòng hỏi-đáp chỉ dành cho dữ liệu tầng căn. Hiện thực: khối KHO DỰ ÁN của `chat-reply`. | M | INS-10 |
| FR-116 | 🟡 **"Căn X của dự án Y còn không?"** Đọc `unit_status`; quá TTL xác nhận 7 ngày thì phải hỏi lại chủ trước khi khẳng định; mã căn không có thì nói thật. Hiện thực: khối CĂN TRONG DỰ ÁN của `chat-reply`. Chưa: căn chuyển đã bán thì báo mọi B đang chờ kèm căn thay thế cùng dự án. | M | INS-10; Cầu Nối §F2 |
| FR-117 | 🟡 **Trang dự án `/du-an/{slug}` + màn quản lý giỏ hàng** cho admin/NMG (giai đoạn 2). Hiện thực: `app/du-an/[slug]` SSG (thông tin dự án, dải giá, lưới tin kèm mã căn và tình trạng, JSON-LD, sitemap). Chưa: màn quản lý giỏ hàng — đổi `unit_status` vẫn qua sửa tin. | S | OPEN-15 (b) |
| FR-118 | 🟡 **Địa giới hành chính hai tên.** Trục chính là bản trước sáp nhập 1/7/2025 trên toàn hệ thống (URL, taxonomy, chat), kèm bảng ánh xạ phường cũ ↔ mới để bot hiểu cả hai cách gọi, hiển thị tên mới khi nói chuyện pháp lý, và tra cứu SEO hai chiều. Chưa: bảng `ward_mapping` — thuộc đợt 2 của FR-174, chờ OPEN-27. | M | INS-12; quyết định chủ dự án 24/08/2026 |
| FR-119 | ⛔ [deprecated → Aioinhadat 2026] **Máy tính lãi vay `/tinh-lai-vay`.** Trang phụ SEO 2024 cũ, không thuộc luồng cốt lõi Aioinhadat. | — | chốt 08/09/2026 OPEN-48 |
| FR-120 | ⛔ [deprecated → Aioinhadat 2026] **Trang `/thong-ke`.** Trang phụ SEO 2024 cũ, không thuộc luồng cốt lõi Aioinhadat. | — | chốt 08/09/2026 OPEN-48 |
| FR-121 | ⛔ [deprecated → interests Zalo] **Lưu tin yêu thích trên web.** Thay bằng lưu sở thích `interests` tự động qua chat Zalo. | — | chốt 08/09/2026 OPEN-48 |
| FR-122 | ⛔ [deprecated → Aioinhadat 2026] **Bản đồ `/ban-do`.** Trang phụ SEO 2024 cũ, không thuộc luồng cốt lõi Aioinhadat. | — | chốt 08/09/2026 OPEN-48 |
| FR-123 | ✅ **Bộ lọc danh sách.** Khoảng giá theo loại giao dịch, khoảng diện tích, số phòng ngủ, sắp xếp — dạng link thuần để URL chia sẻ được. Hiện thực: `/mua-ban`, `/cho-thue` (mở rộng thêm bộ lọc thông số ở FR-172). | M | port từ NhaDat-Radar |
| FR-124 | ⛔ [deprecated → Zalo UID] **Tài khoản NMG magic-link 2024.** Môi giới không cần tài khoản web, làm việc trực tiếp trong Zalo. | — | chốt 08/09/2026 OPEN-48 |
| FR-125 | ⛔ [deprecated → Aioinhadat 2026] **Trang `/moi-gioi`.** Trang phụ 2024 cũ, không thuộc luồng cốt lõi Aioinhadat. | — | chốt 08/09/2026 OPEN-48 |
| FR-126 | ⛔ [deprecated → Zalo UID] **Tài khoản người mua trên web 2024.** Người mua không cần tài khoản web, tương tác 100% qua Zalo. | — | chốt 08/09/2026 OPEN-48 |
| FR-127 | ✅ **Trang `/admin` duyệt tin.** Danh sách `cho_thong_tin` → Duyệt (`dang_ban`) / Ẩn (`an`), kèm số liệu tổng quan; quyền theo bảng `admins` + RLS `listings_admin_*`. | S | quyết định chủ dự án 25/08/2026 |
| FR-128 | ✅ **Cột `listings.bedrooms` + lọc "N+ phòng ngủ".** Backfill bằng regex từ mô tả, phần còn lại do bot bóc khi hỏi S. Hiện thực: `listings.bedrooms`, bộ lọc `/mua-ban`. | S | hoàn thiện FR-123 |
| FR-129 | ✅ **Hỏi nhỏ giọt người bán.** Đăng tin xong hỏi ĐÚNG MỘT thông tin thiếu ưu tiên nhất, trả lời xong hỏi câu kế; im lặng thì cron nhắc theo nhịp với trần: không hỏi khi còn câu chờ, ≤3 câu/24h/tin, ≤10 tin/nhịp, ≤2 căn/24h mỗi seller, chỉ tin <7 ngày hoặc seller có Zalo. Văn phong khen trước — hỏi sau (§6.8). Hiện thực: `ask-seller` + cron `seller-drip-tick`. | M | quyết định chủ dự án 25/08/2026; INS-06, INS-09 |
| FR-130 | ✅ **Khai thác nhu cầu người mua nhỏ giọt, giống người.** Hồ sơ tích luỹ trong `buyers.preferences`, bóc sau mỗi tin, không hỏi lại điều đã biết; hỏi khu vực + khoảng giá trước, gộp được 2–3 ý vào một câu; **đủ khu vực + tầm giá thì ngừng dò**; hỏi 2 lượt liên tiếp thì lượt kế phải đưa giá trị. Trả lời trước — hỏi sau, tối đa 2 bong bóng. Hiện thực: `chat-reply`. | M | quyết định chủ dự án 25/08/2026; UF-04 |
| FR-131 | ✅ **Gộp tin nhắn gõ vụn, không delay nhân tạo.** Có tin mới hơn của cùng khách thì lượt này nhường, chỉ tin cuối chùm trả lời trên toàn ngữ cảnh gộp → một câu trả lời thay vì ba câu chồng nhau; kèm chống đua tạo trùng buyer/conversation. Hiện thực: `ensure_buyer_conversation` (advisory lock) + `messages.seq`. | M | quyết định chủ dự án 25/08/2026 |
| FR-132 | ✅ **Kho dự án, ưu tiên dự án đang phân phối.** Bảng `projects` giữ Ny'ah Phú Định (`priority=1`, `is_partner`) — chủ dự án đang trực tiếp bán; bot trả lời tầng dự án thẳng từ kho, giá từng căn thì phải nói "để em kiểm tra rồi báo"; Ny'ah chỉ giới thiệu chủ động MỘT lần, không lặp khi khách từ chối. Seed mogi đã gỡ, cơ chế nạp giữ ở code. | M | quyết định chủ dự án 25/08/2026; INS-10 |
| FR-133 | ✅ **Nhắc hẹn + hỏi thăm khi im lặng.** (a) Ai hứa "chiều gửi ảnh, mai báo lại" thì đặt hẹn và tới giờ nhắc khéo MỘT tin, họ nhắn lại trước hẹn thì huỷ nhắc; (b) buyer im 5–6 ngày thì hỏi thăm (FR-60…63). Mọi tin chủ động chỉ gửi 8h–21h VN. Hiện thực: `reminders` + `nudge` + cron `nudge-tick`. | M | quyết định chủ dự án 25/08/2026; INS-03 |
| FR-134 | ✅ **Bot đọc ảnh khách gửi (vision).** Mô tả trung thực điều thấy được, đoán thì nói "hình như là…" rồi xác nhận lại, KHÔNG suy diễn vật liệu/pháp lý từ ảnh; ảnh ghi vết trong `messages`. Voice/STT ngoài phạm vi. Hiện thực: `chat-reply` nhận `image_url`, `zalo-webhook` + bridge. Phía NGƯỜI BÁN từ 09/09/2026 ảnh còn được phân loại + cất kho + đọc sổ (FR-185). | S | quyết định chủ dự án 25/08/2026 |
| FR-135 | ✅ **Cờ cần người thật.** Bật khi khách đòi gặp người thật, bức xúc thật sự, đàm phán giá hồi kết, hoặc "để em hỏi lại" lặp 2 lần cùng một chuyện — không bật vì câu hỏi khó thường ngày; bot hứa nhờ người phụ trách nhắn lại. Hiện thực: `conversations.needs_human` + báo cáo CTV. | M | quyết định chủ dự án 25/08/2026 |
| FR-136 | ✅ **CRM CTV — chia đơn xoay vòng.** Bảng `ctvs` bật/tắt bằng cờ `active`; mỗi hội thoại mới được gán CTV đang ôm ít đơn hoạt-động-30-ngày nhất, hoà thì ai nhận cũ nhất nhận trước. Hiện thực: trigger `assign_ctv_round_robin`. | M | yêu cầu chủ dự án 25/08/2026 |
| FR-137 | 🟡 **Báo cáo CTV 17h mỗi ngày.** Tổng hợp per-CTV (đơn đang chăm, đơn tương tác hôm nay, lịch xem sắp tới, đơn chờ người thật) + chấm điểm chăm khách theo rubric 4 tiêu chí, gửi MỘT tin về Zalo cá nhân admin, lưu `ctv_daily_reports` chống gửi đôi. Hiện thực: `ctv-report` + cron 17h VN. Chưa: `ZALO_ADMIN_ZALO_ID`/OA chưa cấu hình nên tin đi đường bridge (FR-149). | M | yêu cầu chủ dự án 25/08/2026 |
| FR-138 | ✅ **Cấu hình "não" bot không cần deploy.** Bảng `bot_prompts` (văn phong, luật phí, nhịp nhắn, kịch bản người bán, từ điển lóng, few-shot, rubric) đè lên mặc định trong code; sửa ở Table Editor là bot đổi ngay lượt sau, không có dòng thì dùng bản `_shared/prompts.ts`. | M | yêu cầu chủ dự án 25/08/2026 |
| FR-139 | ✅ **Vòng đời tin theo trạng thái tiếng Việt.** `cho_thong_tin` (thiếu giá/DT/phường, ẩn khỏi web) → `dang_ban` (đủ 3 trường thì tự lên web) → `dang_quan_tam` (khách hỏi, badge trên web) → `da_chot` (bot báo thật + gợi căn tương tự); thêm `an` (gỡ tay). 7 ngày không ai hỏi thì `dang_quan_tam` tự về `dang_ban`. Hiện thực: `listings.status` + trigger + cron decay. | M | yêu cầu chủ dự án 25/08/2026 |
| FR-140 | ✅ **Fallback hỏi-chủ-nhà → CTV → admin.** Bot hứa "để em hỏi lại chủ nhà" → `info_requests` (chống trùng 24h/căn), trigger định tuyến theo người còn liên lạc được; giao CTV/admin thì sinh việc escalation đi OA hoặc bridge. Chủ trả lời → báo lại khách đúng câu trả lời và huỷ nhắc im lặng cùng căn. SĐT/Zalo admin nằm ở bảng `admins`, không nằm trong code. Nửa "báo admin luôn" đã thay bằng FR-173. | M | yêu cầu chủ dự án 25/08/2026; cập nhật 02–03/09/2026 |
| FR-141 | ✅ **Người thật chăm thì bot nhường sân.** CTV/admin gõ tay từ acc Zalo → ghi tin `sender='human'` + mốc chạm tay; trong **30 phút** bot chỉ ghi log tin khách, không chen ngang; ngừng đủ lâu thì bot tiếp chuyện lại và thấy cả nội dung người thật đã nhắn. Hiện thực: `conversations.human_touch_at`, endpoint `human_note`. | M | yêu cầu chủ dự án 25/08/2026 |
| FR-142 | ✅ **Chốt giao dịch theo tín hiệu đồng ý.** Tin trước có đề nghị chốt/cọc và khách đồng ý bằng chữ, emoji, like/tim hay sticker → ghi `deals` (phí 1% CCRB / 0.5% NMG), tin sang `da_chot`, báo gấp CTV/admin. Đồng ý lịch xem dùng cùng bộ tín hiệu. Bộ tín hiệu cấu hình ở `bot_prompts.agree_rules`; chống ghi trùng theo cặp căn + khách. | M | yêu cầu chủ dự án 25/08/2026 |
| FR-143 | ✅ **Bot gửi hình thật cho khách.** Nguồn là ảnh kho cộng ảnh chính chủ gửi qua chat; khách xin hình mà model quên thì hệ thống tự đính kèm theo mã khách nhắc, tối đa 4 hình/lượt; căn chưa có hình thì đi đường hỏi-chủ-nhà. Hiện thực: `send_photos` + mảng `photos`, bridge/OA gửi ảnh đính kèm. | M | yêu cầu chủ dự án 25/08/2026 |
| FR-144 | ✅ **Vòng thu thập thông tin chính chủ trọn đời tin.** (a) Câu rao mới → tự tạo tin nháp + hỏi ngay MỘT thông tin thiếu; (b) mỗi câu trả lời lưu fact và chảy vào cột; (c) tin đủ điều kiện thì NGỪNG hỏi và báo "tin đã lên web"; (d) khách quan tâm hỏi thêm thì mở vòng mới; (e) đang có câu chờ thì nhịp cron bỏ qua, không hỏi chồng. Hiện thực: `chat-reply` + `ask-seller`. | M | yêu cầu chủ dự án 25/08/2026 |
| FR-145 | ✅ **Widget Zalo trên web.** Nút nổi góc trái dưới trên mọi trang; riêng trang chi tiết căn ẩn widget, thay bằng nút "Chat Zalo về căn này" mang ngữ cảnh nằm trong bài. Link cấu hình một chỗ. Hiện thực: `components/ZaloWidget` ở `app/layout.tsx`. | M | yêu cầu chủ dự án 25/08/2026 |
| FR-146 | ✅ **Trần 100 tin/24h mỗi khách.** Vượt trần thì bot gửi ĐÚNG MỘT tin hẹn người phụ trách nhắn lại, bật cờ cần người thật, sinh việc cho CTV; các tin sau trong 24h bị bỏ qua im lặng, không tốn lượt model. Mục đích kép: chặn đốt tiền model và chuyển khách nhắn quá nhiều sang người thật. | M | yêu cầu chủ dự án 25/08/2026 |
| FR-151 | ✅ **Hai cổng chặn đốt tiền model.** (a) Trần TOÀN CỤC theo ngày: vượt thì trả 429 và im hẳn, báo admin đúng một lần trong ngày; trần chỉnh bằng secret, mặc định 1000/ngày. (b) Bí mật dùng chung `x-bridge-secret` cho tám edge function; chưa đặt secret thì chạy như cũ. Hiện thực: `bot_usage` + `bump_model_quota()`, `_shared/gate.ts`. | M | soát rate limit 27/08/2026 |
| FR-147 | ✅ **Leo thang cần-người-thật: CTV → admin.** Bật cờ → việc escalation gán CTV đang chăm đơn (chống báo lặp). Quá **30 phút** chưa ai gõ tay → việc thứ hai không gán CTV, rơi về admin, đánh dấu để không báo lại. Người thật vào chat thì cờ tự hạ và các nhắc đang chờ bị huỷ. Hiện thực: `nudge` + endpoint `human_note`. | M | yêu cầu chủ dự án 25/08/2026 |
| FR-148 | ✅ **Kho ảnh thật cho tin rao.** Ảnh nằm trên Supabase Storage, view `listing_photos_v` là cửa đọc chung cho web (ảnh bìa + gallery, chưa có thì rơi về ảnh minh hoạ) và cho bot (gộp ảnh kho, ưu tiên trước ảnh chủ gửi qua chat). Lối neo theo MÃ TIN và xếp theo tên file đã thay bằng FR-165 (UUID + `sort_order`). | M | yêu cầu chủ dự án 25/08/2026 |
| FR-149 | ✅ **Mọi báo cáo cuối đường về Zalo cá nhân admin.** Admin không dùng OA nữa: đích cuối của escalation (FR-140/147), cảnh báo trần (FR-146) và báo cáo CTV 17h (FR-137) là số Zalo trong `admins.zalo_phone`, không nằm trong code. Bridge kéo việc, resolve SĐT → uid, nhắn rồi ack; còn OA thì vẫn ưu tiên gửi thẳng. | M | yêu cầu chủ dự án 25/08/2026 |
| FR-150 | ✅ **Thiếu dữ liệu thì suy giảm êm, không chết lặng.** (a) Loại BĐS tự trích từ câu chữ, chỉ hỏi khi không đoán nổi; đoán không ra thì để giá trị thật `chua_ro` chứ không NULL ngầm, và trả lời vẫn không rõ thì hỏi lại kèm lựa chọn, tuyệt đối không ghi fact bừa. (b) Điều kiện lên web treo trên chính bảng `listings`. (c) Giá tính lại cả khi sửa. (d) Bridge resolve được uid thì ghi ngược vào hồ sơ. | M | soát cấu trúc dữ liệu + yêu cầu chủ dự án 26/08/2026 |
| FR-152 | ✅ **Sổ lỗi bền + nhịp tim bot.** (a) `bot_errors` + `bot_health` (admin đọc được). (b) Nhịp quét phản hồi HTTP, chép mọi cái không 2xx sang sổ, báo admin gộp một tin mỗi giờ. (c) Nhịp tim bridge, im quá 15 phút trong 7–22h thì ghi sổ. (d) Cửa ghi lỗi tầng ứng dụng `log_loi`/`ghiLoi` cho mọi `catch` — lỗi nguy nhất lại TRẢ 200. (e) Cảnh báo đi thẳng ntfy, không qua bridge. Sổ tự dọn sau 30 ngày. | M | soát cloud/compute 27/08/2026; soát 04/09/2026 |
| FR-153 | ✅ **Câu trả lời nhỏ giọt chảy ngược vào cột có cấu trúc.** Fact `so_phong_ngu`, `dien_tich*`, `tang`, `huong` đổ vào cột tương ứng của `listings` để web lọc được; cố ý KHÔNG đụng `description` vì câu rao gốc là văn phong người bán. Luật ghi đè theo bậc nguồn (FR-164) và câu mới nhất của chủ nhà thắng (FR-163). Hiện thực: trigger `listing_facts_sync_cols`. | S | yêu cầu chủ dự án 27/08/2026 |
| FR-154 | ✅ **Bóc giá tiếng lóng ra số VND.** `parse_vnd()` hiểu `tỏi`, `củ`, `rưỡi`, `5t5`, `6ty2`, `4ty` ngoài `tỷ/triệu/tr`. `price_raw` giữ NGUYÊN VĂN người rao, `price_vnd` chỉ là bản dịch máy đọc được nằm cạnh — bộ lọc giá của web đọc cột số. Cố ý KHÔNG quy `cây`/`lượng` vàng ra tiền (tỷ giá đổi mỗi ngày, ghi số vào là bịa). | M | yêu cầu chủ dự án 27/08/2026 |
| FR-155 | ✅ **Hạng người rao Đồng / Bạc / Vàng.** Tính TẠI CHỖ từ số tin, không lưu thành cột (cột không ai cập nhật sẽ đóng băng rồi nói dối). NMG: Vàng từ 10 tin đang rao và tỉ lệ chốt ≥5%, Bạc từ 5 tin hoặc ≥1 căn đã chốt. CCRB: Vàng khi đã chốt được căn, Bạc khi tin đủ thông tin lên sàn. View chỉ lộ tên + số đếm + hạng. Chưa: ngưỡng chốt cuối (OPEN-26). Điểm NGƯỜI RAO (0–100, theo chat Gemini 21/06) tách riêng ở FR-183; hạng theo điểm (Đồng <50 / Bạc 50–79 / Vàng ≥80 của chat) chưa chốt thay công thức này — OPEN-26. | S | yêu cầu chủ dự án 27/08/2026 |
| FR-156 | ✅ **Admin tự đăng tin theo nguồn** (`/admin/dang-tin`) cho tin không đi qua Zalo. Mọi trường chọn được là ô xổ xuống; giá gõ y như người rao nói và hiện ngay số máy đọc ra (gọi thẳng RPC, không chép luật sang JS); SĐT và Zalo mỗi cái một ô tích riêng — không tích thì không lưu. Chạy qua RPC tự kiểm quyền; trùng Zalo/SĐT thì dùng lại người bán cũ. | M | yêu cầu chủ dự án 27/08/2026 |
| FR-157 | ✅ **Neo hội thoại người bán theo CĂN, tách vai từng lượt.** (a) Câu hỏi đang chờ chọn theo độ tin cậy: mã tin chủ tự nhắc > căn bot vừa hỏi > câu mới nhất — trước đây fact của căn A ghi thẳng vào căn B mà không bao giờ tự lộ. (b) Neo đặt ở trigger DB vì câu hỏi sinh ra từ bốn đường. (c) Câu hỏi drip vắt vai mã căn + tên đường. (d) Có hồ sơ bán không có nghĩa cả đời chỉ được bán. | M | soát cấu trúc 27/08/2026; OPEN-22 |
| FR-158 | ✅ **Câu rao sinh mã tin ngay, cả hệ chỉ còn MỘT dãy mã.** (a) Cổng rao nhận cả ý định rao theo thứ tự từ ("muốn bán", "bán nhà") chứ không đòi phải có chi tiết — câu rao thật thà nhất từng bị trượt. (b) Một dãy `BDS-Q5-####` duy nhất cho thứ khách ĐỌC QUA ZALO. (c) Bộ sinh mã về DB (trigger + advisory lock) thay hai bản chép tay. | M | yêu cầu chủ dự án 27/08/2026 |
| FR-159 | ✅ **Phân vai bằng câu hỏi mở đầu, mặc định là người mua.** Người lạ không tự nhận có BĐS thì bot HỎI "muốn mua hay đang có bất động sản cần rao ạ?" (không gọi model); ai không nói rõ thì Ở LẠI hàng người mua vì đoán nhầm chiều đó rẻ hơn. Hồ sơ bán chỉ mở khi tự nhận có BĐS, và nhãn gán ngay lúc bóc tách: có nhà = chính chủ, chỉ ai tự xưng môi giới mới là NMG. **09/09/2026 tối: KHÔNG báo nhãn, KHÔNG báo phí cho người bán nữa** ("sẽ không báo nhãn và mức phí cho người bán nữa, chỉ tự ghi nhận thôi, chỉ báo đã bóc tách được gì ở giai đoạn testing này") — nhãn gán im lặng, người bán chỉ thấy bong bóng "📝 Em ghi nhận: …" liệt kê thứ đã bóc; phí vẫn chỉ nói khi họ hỏi (FEE_RULES). Admin vẫn nhận việc 🆕 kèm nhãn + mức phí. (Trước đó 02/09–09/09 chiều: báo một câu nhãn không kèm phí.) | M | quyết định chủ dự án 27/08, 02/09 và 07/09/2026 |
| FR-160 | ❌ **Từ 3 tin rao trở lên thì là môi giới.** Suy `seller_type` từ số tin đang rao thay vì lời tự khai. Chưa dựng: `seller_type` đang là thứ tính phí nên một chính chủ mở tin thứ ba là phí tự rơi một nửa — chờ OPEN-28 chốt phí trước. | M | quyết định chủ dự án 27/08/2026 |
| FR-161 | ✅ **Bot nghe được tiếng Việt KHÔNG DẤU ở mọi tầng cổng.** Chuẩn hoá bỏ dấu MỘT lần rồi khớp, không vá từng mẫu. Hai chế độ: tin có dấu đi bộ regex có dấu như cũ (dấu của người gõ là thông tin — "đang bàn" không phải "đang bán"), tin không dấu mới rơi về bộ đã bỏ dấu. Model luôn đọc chữ gốc; `description`/`price_raw` lưu nguyên văn. | M | quyết định chủ dự án 27/08/2026 (OPEN-29) |
| FR-162 | ✅ **Sổ idempotency cho tin đến.** Mỗi `zalo_msg_id` một vòng đời `received → processing → completed/failed/dead`. Retry cùng mã tin thì PHÁT LẠI y nguyên câu trả lời đã lưu, không gọi model và không đốt quota; hai bản sao đồng thời thì bên thua nhận `in_flight`, không trả lời đôi. Tách bốn danh tính: sự kiện inbound, tin logic, job xử lý, tin gửi ra; thứ tự tin tất định bằng `messages.seq`. | M | yêu cầu chủ dự án 27/08/2026 |
| FR-163 | ✅ **Toàn vẹn dữ liệu ở tầng DB.** Nguồn sự thật: `listings` = giá trị hiện hành, `listing_facts` = bằng chứng hội thoại append-only, `price_vnd` dẫn xuất từ `price_raw`. Bỏ anti-pattern "chỉ ghi khi NULL" — câu trả lời mới nhất của chủ nhà thắng; thêm ràng buộc chặn dữ liệu vô lý cho `deals`, `viewings`, `conversations`, và guard trạng thái kết cho `reminders`/`inbound_ledger`. | M | yêu cầu chủ dự án 28/08/2026 |
| FR-164 | ✅ **Đường ống dữ liệu tin rao — một nhà chức trách cho mỗi chặng.** Chặng chuẩn: bằng chứng → fact → chuẩn hoá → cột → đánh giá đủ tin → quyết định đăng. Bậc nguồn `chu_xac_nhan` > `admin` > `suy_doan` (lời chủ mà thua admin thì cả vòng drip vô nghĩa — treo OPEN-31). Một cửa ghi fact duy nhất, một quyết định đăng tin duy nhất trên `listings`, có cả đường HẠ kệ khi tin mất thông tin. | M | yêu cầu chủ dự án 28/08/2026 |
| FR-165 | ✅ **Kho ảnh neo vào ID bất biến, có bảng media thật.** Đường dẫn `listing-public/<listing_id>/<media_id>`, ràng bằng CHECK — neo theo mã tin thì đổi mã là ảnh rơi khỏi tin âm thầm. Bảng `listing_media` với khoá ngoại thật; hai bucket (công khai chỉ ảnh, riêng thêm PDF) và CHECK buộc giấy tờ nằm bucket riêng; thứ tự tất định + ảnh bìa do trigger giữ; xoá file qua hàng đợi thử lại được. | M | yêu cầu chủ dự án 29/08/2026 |
| FR-166 | ✅ **Việc chạy nền tin cậy.** Giữ ĐƯỜNG NHANH (webhook trả lời inline, độ trễ như cũ) và thêm ĐƯỜNG CỨU quét việc bỏ rơi mỗi phút — instance chết sau khi ack là tin khách mất vĩnh viễn, im lặng. Một luật lùi dần dùng chung cho ba hàng đợi, có trạng thái kết `dead` để không thử mãi; nhắc/nudge giành việc atomic để hai lượt chạy chồng không cùng gửi; đếm bong bóng đã gửi để không gửi đúp. | M | yêu cầu chủ dự án 29/08/2026 |
| FR-167 | ✅ **Soát bảo mật theo VAI THẬT.** Không đọc policy rồi suy diễn mà ĐÓNG VAI `anon`/`authenticated` thật rồi thử đọc/ghi/gọi. Bịt 6 lỗ, 3 khai thác được từ Internet: edge function không kiểm gì, `verify_jwt=true` chỉ đòi khoá công khai (nằm sẵn trong bundle JS), ảnh và mã của tin CHƯA ĐĂNG lộ qua bảng media và view. Bài học: gắn cổng cho một function thì phải liệt kê MỌI người gọi, kể cả trigger. Chưa: webhook Zalo chưa kiểm chữ ký (OPEN-33). | M | yêu cầu chủ dự án 29/08/2026 |
| FR-168 | ✅ **Chuông báo hết tiền tài khoản AI.** Hết credit thì bộ não câm hoàn toàn nhưng lỗi nằm lẫn giữa lỗi model thường ngày. Bắt bằng trigger trên sổ lỗi (không phải sửa edge function), ghi thẳng không qua van chống ngập (chuông bị van nuốt đúng lúc sổ đang ngập là vô dụng), không qua đường escalation (đường đó cũng có thể đang chết), tự hãm nhịp 6 giờ. | M | yêu cầu chủ dự án 01/09/2026 |
| FR-169 | ✅ **Đo tiền bộ não theo CHỮ.** Ghi số chữ vào/ra và chữ nhớ tạm cho mọi lượt gọi model, lưu số chữ chứ không lưu thành tiền (giá đổi thì sửa hằng số, số chữ đã ghi vẫn đúng mãi); admin đọc ở `/admin`. Cắt hai chỗ rò: nhịp nhớ tạm sai (lượt nào cũng trượt, mà trượt còn đắt hơn không dùng) và khối dự án cố định nằm nhầm nửa tính đủ giá. Nguyên tắc: tách hai nửa prompt theo GIÁ chứ không theo chủ đề. | M | yêu cầu chủ dự án 01/09/2026 |
| FR-170 | ✅ **Soát bóc dữ liệu theo bốn vai người nhắn.** Vá: người bán bị rẽ nhầm sang nhánh mua (5/7 câu chủ nhà thường nói); khoảng giá đọc sai ba cách nói rất thường ("5 tỷ 8" thành 5 tỷ, "từ 5 đến 6 tỷ" thành cận dưới); tin CHƯA ĐĂNG và ĐÃ GỠ lộ qua mã căn đoán được; mã căn viết thường sinh lịch xem thứ hai; ảnh kèm chú thích rơi mất; phí tính sai cho người bán chưa phân loại; và bốn lỗi nữa bắt được bằng cách bơm tin qua handler thật. | M | yêu cầu chủ dự án 01/09/2026 |
| FR-171 | ✅ **Tối ưu chi phí vận hành, không đổi hành vi với khách.** Bot chạy rỗ khi im (cron chỉ chạy giờ người, dọn sổ cron cũ, giữ cửa hàng đợi bằng đúng vị từ của hàm nhận việc); một tin khách nhắn tốn ít vòng DB hơn (nhớ tạm ở tầng module, gộp truy vấn `messages`, hậu kỳ chạy song song); chữ-máy gửi model ít đi (kho chỉ nạp khi hồ sơ đủ khu vực + giá, cắt fact và tin khách quá dài); web tải nhẹ hơn (chọn cột thay `select *`, gộp truy vấn `/admin`, ảnh lazy). Hạ tầng dùng chung: một cổng bí mật cho bảy function. Cố ý CHƯA làm vì đổi kiến trúc: gộp webhook vào bộ não (OPEN-34), thay lượt model của nhắc bằng mẫu câu (OPEN-35). | S | yêu cầu chủ dự án 02/09/2026; số đo `[giả định BA — đo trên DB thật + e2e]` |
| FR-172 | ✅ **Tin rao CÓ CẤU TRÚC chuẩn sàn.** Dữ liệu vốn có nhưng nằm trong một cục chữ: web không lọc được, bot đi "hỏi lại chủ nhà" thứ tin rao đã ghi, vòng drip hỏi lại đúng thứ chủ nhà vừa viết. (a) Thêm cụm cột thông số: đường vào và độ rộng hẻm, kích thước ngang/dài/nở hậu, DT công nhận/xây dựng, số tầng, WC, pháp lý, hoàn công, quy hoạch, thang máy, xe hơi vô nhà, căn góc, nội thất, năm xây, thương lượng, đang cho thuê, giá/m². (b) Bóc từ mô tả bằng luật, ghi nguồn `boc_mo_ta` (bậc thấp nhất). (c) Fact chủ nhà đè theo bậc nguồn FR-164. (d) Cột đã có thì bot KHÔNG hỏi lại. (e) Backfill toàn kho. (f) Web thêm bảng thông số + bộ lọc. Cố ý chưa: bóc bằng model (regex đã bắt 70–85%). | M | yêu cầu chủ dự án 02/09/2026; số đo `[giả định BA — đo trên DB thật]` |
| FR-173 | ✅ **Câu khách hỏi đi về chủ nhà trước, 12 giờ không trả lời mới về CTV; CTV chậm thì rớt hạng và admin đỡ khách.** (a) [sửa chủ dự án 09/09/2026 chiều (đối chiếu chat Gemini "AI Ơi Nhà Đất" 21–27/06/2026, chốt từng dòng OPEN-55) — "bot chủ động hỏi chủ nhà, nếu chủ nhà sau 12 tiếng không thấy hồi âm thì hỏi CTV"; thay bản 03/09 "không giao chủ nhà nữa"] Câu hỏi nguồn khách (`buyer_ask`) giao **CHỦ NHÀ** nếu chủ có Zalo: `route_info_request` đặt `assignee='seller'`, `sla_due_at = now() + chu_nha_han_gio()` (`app_config.chu_nha_han_gio`, mặc định 12), `notify_info_request_escalation` xếp tin "💬 Anh/chị ơi, có khách đang hỏi căn [địa chỉ]: …" vào `reminders` (bridge gửi nguyên văn); chủ trả lời trong chat → nhánh bán ghi fact + `answered` → trigger báo lại khách (FR-140 c). Quá hạn: `info_request_sla_tick` (a) đóng dấu `chu_nha_qua_han_at`, chuyển sang CTV ít việc nhất với hạn 120 phút (không có CTV → admin). Tin không có chủ Zalo thì giao CTV ngay như cũ; câu nhỏ giọt (`seller_flow`) vẫn đi chủ nhà. (b) Nhắc CTV kèm mẫu trả lời `#mã tin: câu trả lời`, admin không bị báo mỗi câu. (c) Hạn **120 phút**, quá hạn thì báo admin đỡ khách đúng một lần. (d) CTV/admin nhắn theo mẫu thì bot ghi fact nguồn nội bộ, đóng câu hỏi và báo lại khách, không tốn lượt model. (e) Hạng CTV theo tỉ lệ trả lời đúng hạn 30 ngày: Vàng ≥90%, Bạc ≥70%, dưới 3 câu là chưa đủ dữ liệu. | M | quyết định chủ dự án 03/09/2026 |
| FR-174 | 🟡 **Địa bàn mở — Sài Gòn (phường mới) + Long An, trọng tâm bán.** Đợt 1 (xong): bot bóc quận/huyện từ chính câu rao thay vì ghi cứng "Quận 5", form admin và `/quan-ly` có ô quận/huyện, bot tự giới thiệu địa bàn mới, web nói đúng địa bàn; `/thong-ke` và danh sách phường vẫn theo kho thật. Mã tin `BDS-Q5-####` không đổi. Chưa (đợt 2, chờ OPEN-27 nửa sau): bảng `wards` một nguồn (tên mới ↔ cũ, quận cũ, tỉnh, toạ độ tâm) thay các danh sách phường và toạ độ ghi cứng, tag SEO theo khu vực mới, ánh xạ FR-118, drip hỏi phường theo địa bàn. | M | quyết định chủ dự án 03/09/2026 |
| FR-175 | ✅ **Rổ hàng đọc như Excel, không cần vào Supabase.** Chủ dự án và sếp mở Schema Visualizer thấy 31 bảng + 17 view "rối, khó kiểm soát" (07/09/2026); thứ họ muốn nhìn là sheet Excel gốc 9 cột. Hiện thực hai tầng cùng một bố cục cột: (a) schema `so` trên DB — `so.ro_hang` + `so.nguoi_ban` (`20260907c`, có SĐT, chỉ Dashboard/service_role đọc; `20260907f` ô đọc một dòng: mô tả gộp xuống dòng thành ` · `, vị trí bỏ đuôi `, Quận 5, Hồ Chí Minh`, diện tích dấu phẩy — soát dữ liệu thật 07/09: 163/174 mô tả có CRLF, 146/174 vị trí thừa đuôi); (b) trang `/admin/ro-hang` — bảng lọc bán/thuê + trạng thái, tìm không dấu, sắp theo cột, 20 dòng/trang (FR-80), nút tải CSV có BOM mở thẳng bằng Excel; có cột **SĐT người bán** (`sellers.phone`, chỉ admin thấy nhờ RLS `sellers_admin_read`, người khác nhận null — quyết định chủ dự án 07/09/2026; NFR-07 chỉ cấm SĐT của khách B), mô tả qua `sanitizeDescription` (FR-104); trình bày ba cột y như `so.ro_hang` sau `20260907f` (mô tả một dòng, vị trí bỏ đuôi thành phố, diện tích dấu phẩy) để hai kính nhìn không lệch nhau. (c) **Trang `/admin/ro-hang/json`** [nguồn: chủ dự án 09/09/2026 — "thêm 1 trang lưu dưới dạng json"]: mỗi tin một khối JSON (mã, loại giao dịch, vị trí, diện tích, giá, gấp, trạng thái, người bán, mốc duyệt/nói đủ, `boc_tach` FR-177 h), bỏ trường trống; tìm không dấu kể cả trong nội dung bóc tách; lọc "chỉ tin có bóc tách"; nút tải cả rổ (theo bộ lọc) thành `.json`, nút chép JSON từng tin; link từ nút `{ } JSON` trên `/admin/ro-hang`. (d) **Khung CRM** [chủ dự án 09/09/2026 — "lấy giao diện của ZaloCRM cho dễ nhìn", bố cục theo github.com/locphamnguyen/ZaloCRM v3.4 (AGPL — chỉ lấy bố cục, không lấy mã)]: `app/admin/layout.tsx` + `components/AdminShell.tsx` — thanh trên navy: Bàn làm việc · Khách hàng · Tin nhắn · Rổ hàng · Mẫu câu · Báo cáo · Vận hành, ô tìm (→ `/admin/ro-hang?q=`), nút Đăng tin, chuông (việc chờ + lỗi bot 24h), avatar/đăng xuất; tab đi theo URL (`/admin?tab=`); Header/Footer công khai ẩn dưới `/admin`; `/admin/tin-nhan` màn chat 3 cột (lọc · hội thoại · tin) có sửa câu chuẩn tại chỗ. (e) **Chuẩn NN/g + nút Xoá khách** [chủ dự án 09/09/2026 — "thiết kế lại cho chuẩn NN Group, bỏ hết yếu tố làm web giống AI; thêm nút xoá khách"]: bộ `components/ui.tsx` (Btn/Card/Badge/Alert/PageHeader/EmptyState) + icon SVG thay toàn bộ emoji, bỏ pill/bóng/nhấp nháy trên `/admin/*` (luật ở `06 §6.11`); `/admin/tin-nhan` thêm Làm mới, Đã xử lý (hạ `needs_human`), **Xoá khách** → RPC `admin_xoa_khach(p_zalo)` (`20260909f`, kiểm `la_admin()`, bọc `reset_nguoi_test`, không đụng `mau_cau`), xác nhận nêu hậu quả, báo số dòng đã xoá. Chưa: xuất `.xlsx` tự động lên OneDrive. | M | phản hồi sếp qua chủ dự án 07/09/2026; chủ dự án 09/09/2026 |
| FR-176 | ✅ **Giọng người bán tự nhiên — câu sau bám câu trước, câu lệch thì hỏi lại.** Lượt rao #BDS-Q5-0174 (15:37 07/09/2026) cho thấy 4 lỗi cùng gốc: nhánh hỏi người bán gọi model từng lượt CỤT, không lịch sử, câu lệnh ép khuôn "khen + mã căn + hỏi + lý do vì khách" nên 4 tin liền cùng một khuôn; "kêu chị nha" sống đúng một lượt; bong bóng phí gắn cứng bắn ngay tin đầu; và MỌI câu chat bị ghi làm đáp án ("Kêu chị nha" → pháp lý, "16m nha" → hướng, "Ngang 5" đóng câu diện tích). Yêu cầu: (a) 8 tin gần nhất + cách gọi đã dặn (`sellers.xung_ho`, `20260907d`) vào mọi lượt model của nhánh người bán; (b) khen chỉ khi có gì đáng khen, lý do "vì khách" tối đa 1 lần / 3 tin, không mở mọi tin bằng "Dạ"; (c) mã căn chỉ nhắc khi người bán có ≥ 2 tin (FR-157 c thu hẹp); (d) phí không nói lúc gán nhãn, nói khi được hỏi (FEE_RULES) và một câu lúc tin lên web — admin vẫn nhận nhãn + phí (FR-159); (e) câu trả lời phải KHỚP câu hỏi mới ghi fact: tầng tiền định `_shared/extraction/khop-cau-tra-loi.ts` phân loại `khop / xung_ho / ack / hoi / lech`, lệch thì không ghi, không đóng câu hỏi, hỏi lại bằng lời khác; "Ngang 5" khi hỏi diện tích ghi đúng fact `mat_tien` và giữ câu diện tích treo; **09/09 tối (chạy 12 kịch bản thật): câu trả lời bị ghi LỆCH MỘT Ô hàng loạt** — "Hẻm 4m" vào diện tích, "Đúc 5 tầng" vào phòng ngủ, "lên thổ cư 300m2" vào địa chỉ, "cọc 2 tháng" vào diện tích, "doanh thu 120 triệu" đè GIÁ 45 tỷ — vì các nhánh chỉ hỏi "có số không"; nay `phanLoaiCauTraLoi` hỏi `nhanDienFact` TRƯỚC (nhận ra fact khác họ → lệch + chuyển sang, họ fact trong `HO_FACT`), `vi_tri` cần dấu hiệu địa chỉ thật (không nhận số suông), giá cắt đúng đoạn gốc có dấu (`boDauGiuDoDai`), `nhanDienNhieuFact` tách theo dấu phẩy để "Đường 12m, hướng Bắc" ghi CẢ HAI; model r2/r2b bị buộc hỏi ĐÚNG câu hệ thống đang chờ (không tự nhảy sang câu kế); cổng câu rao nhận đủ loại (kho xưởng, toà nhà, CHDV…) và câu môi giới "em là môi giới, có căn … 5 tỷ", "còn căn nữa …"; `20260909k` ngang×dài → m², địa chỉ gọt dấu phẩy kép; (f) **thứ tự hỏi theo chuỗi liên quan** (`20260907e`; soát 09/09/2026: câu cũ ghi nguồn "Gemini lượt 93" — chat share chỉ có ~50 lượt và không có thứ tự đó, chat 07/09 có 6 lượt; thứ tự thật lấy từ kịch bản Gemini 07/09 lượt 3 + chat 21/06 lượt 19–21, và đã bị FR-177 a / FR-186 thay): nhà phố hẻm rộng/xe vào → mấy lầu mấy phòng → diện tích (sổ) → pháp lý → phường → giá (hướng/quy hoạch/năm xây đã rời vòng hỏi theo FR-177 a) — trước đó 4 câu cùng priority 1 nên thứ tự là A→Z; (g) **hỏi giá và phường**: `gia`/`phuong` vào `required_facts` cho mọi loại BĐS, view `listing_missing_facts` coi "đã có" khi cột `price_vnd`/`ward` đã điền — trước đó không ai hỏi giá nên tin rao từ chat không bao giờ đủ điều kiện `listings_autopublish`. Kiểm: 49 ca đơn vị + 7 ca e2e G1–G7 tái hiện đúng lượt rao đó. Chưa (ngoài phạm vi): điểm uy tín/gamification (OPEN-20), nút quick-reply Zalo, keep-alive nhiều ngày, tên trợ lý T•ai (docs chốt Thái). | M | sếp qua chủ dự án 07/09/2026; log bridge 15:36–15:40; Gemini share "AI Ơi Nhà Đất" |
| FR-177 | ✅ **Nhận ký gửi như môi giới giỏi: hỏi một thứ, khích lệ một câu, chốt bằng bản nháp tin có điểm.** **Mở rộng 09/09 tối** [nguồn: chủ dự án 09/09/2026 tối, sau khi chạy 12 kịch bản chat trên production]: (j) **bản nháp phải rõ ràng và tốt như MỘT TIN RAO THẬT, không bịa** — mọi dòng từ cột hoặc fact chủ nhà đã nói: tiêu đề LOẠI + BÁN/CHO THUÊ (+ CẦN BÁN GẤP khi `gap`), 📍 địa chỉ gọn (bỏ mảnh trùng), 📐 diện tích (m², ngang×dài, nở hậu, thổ cư, hình dáng), 🏗 kết cấu (tầng, tầng ở, PN, WC, thang máy, sân vườn, hiện trạng, năm xây, căn góc, view), 🛣 đường vào (hẻm/mặt tiền, cách mặt tiền, hẻm thông, ngập, hạ tầng, container), 🧭 hướng, 📜 pháp lý (sổ, hoàn công, quy hoạch, thế chấp, xây dựng, sở hữu, thời hạn, thuê đất, lên thổ cư), 🏢 khai thác (toà nhà: phòng, lấp đầy, doanh thu, PCCC; kho: cao, tải trọng, điện, nước thải), 🌱 đất (nước, ranh, mật độ, tầng cao), 🛋 nội thất + đang ở/thuê, 📝 điều kiện thuê (cọc, thời hạn, trượt giá, fit-out, phí QL, gửi xe, điện nước, giờ giấc), 🏫 tiện ích gần, 💡 phù hợp (CHỈ khi chủ nói — bỏ đoán `goiYTiemNang`), 💰 giá (+ còn thương lượng / cố định, lý do bán), 📷 số ảnh, 👉 gọi hành động; (k) **hỏi bù GOM MỘT LẦN 2–3 thông tin** — "seller-hoi-bu-tick sau 5 phút khi khách ngừng thì hỏi thêm 1 lần 2 đến 3 thông tin còn thiếu, ưu tiên thông tin quan trọng hoặc ảnh và sổ hồng, chứ không phải mỗi 5 phút đều hỏi": `ask-seller` drip gom 2–3 câu trong một tin, ưu tiên ảnh/sổ/giá/diện tích/vị trí rồi cơ bản → chuyên môn → sau đăng; `seller_hoi_bu_tick` giữ một lần sau 5 phút; `seller_drip_tick` MỘT lượt/24h/tin thay vì 3 (`20260909l`); (l) **chủ nhà hứa "tối gửi ảnh" lúc đang chờ duyệt** là lời hứa, không phải lời sửa — không gửi lại bản nháp; (m) **sửa thông tin giữa chừng mà đang có câu chờ** thì ghi xong vẫn hỏi tiếp, không im; (n) **nguyên tắc bất đối xứng** [nguồn: chủ dự án 10/09/2026 00:20 "khách thì có thể đưa nhiều thông tin 1 lần và bot thì cứ hỏi nhỏ nhẹ 1 2 vấn đề liên quan một thôi, chỗ cần thêm bong bóng chat có thể xuống dòng để nhắn câu chốt"]: chủ nhà nhắn một câu dài mang 5–6 thông số thì hệ thống bóc HẾT (`nhanDienNhieuFact` + fact phụ PN/WC/hướng/ngang/nở hậu), không bắt họ nói lại; bot mỗi lượt vẫn chỉ hỏi 1–2 ý liên quan nhau, giọng nhẹ; muốn thêm một câu chốt thì XUỐNG DÒNG trong cùng bong bóng, không bắn bong bóng thứ hai. Kịch bản "một câu dài đủ mọi thứ" chưa nằm trong 12 kịch bản TS-KYGUI-34 — ghi để chạy ở đợt sau (chủ dự án ngưng test 10/09). [nguồn: sếp, `Kịch bản huấn luyện môi giới (Gemini 07-09-2026).md` lượt 3–4; chốt OPEN-49]. Thay thứ tự FR-176 f. (a) **Thứ tự hỏi = cơ bản trước, rồi bám theo câu khách đang nói** [nguồn: chủ dự án 07/09/2026 tối]: nhóm CƠ BẢN làm rõ trước — loại BĐS, vị trí (đường + quận/phường), diện tích, giá — thứ tự trong nhóm linh hoạt theo thứ khách vừa nhắc (khách đang nói mặt tiền mấy mét thì hỏi tiếp chiều ngang/dài, chưa nhảy sang giá); xong nhóm cơ bản mới tới hẻm rộng mấy mét, ô tô vào không → mấy tầng, mấy phòng (+WC) → pháp lý: sổ hồng riêng, hoàn công, cầm tay hay ngân hàng → xin ảnh sổ, mặt tiền, hẻm → CTV hẹn khảo sát/quay video (kịch bản Gemini lượt 3). Hiện thực: `required_facts.priority` xếp nhóm cơ bản = 1, còn lại theo chuỗi; câu kế chọn trong nhóm ưu tiên cao nhất còn thiếu, ưu tiên fact CÙNG NHÓM với fact vừa ghi; `huong`/`quy_hoach`/`nam_xay` rời vòng hỏi đầu, chỉ nhặt khi chủ tự kể; "hoàn công", "cầm tay/ngân hàng", "nở hậu" chưa có fact riêng → thêm fact hoặc gộp vào `phap_ly`/`mat_tien` *[giả định BA]*. (b) **Mỗi câu trả lời được đáp bằng một câu khích lệ CÓ NGHĨA gắn với khách mua/thanh khoản** ("hẻm ô tô khách rất chuộng", "nở hậu khách đầu tư thích", "pháp lý chuẩn giúp khách chốt cọc nhanh") rồi mới hỏi câu kế; một thông tin/lần; không khen suông, không lặp mẫu — giữ luật FR-176 b/e. (c) **Đủ thông tin thì gửi BẢN NHÁP TIN** (tiêu đề · vị trí · thông số · kết cấu · pháp lý · tiềm năng sử dụng · giá · gọi hành động = nhắn Zalo #mã, không SĐT — DH-02) kèm lời hứa rao tích cực và hỏi "như vậy được chưa?"; chủ đồng ý (AGREE_RULES) → tin lên kệ; chủ sửa → ghi fact rồi gửi lại. (e) **Hỏi một đường, trả lời một nẻo thì VẪN GHI** [nguồn: chủ dự án 07/09/2026 tối — "vẫn phải ghi nhận câu trả lời của khách để ghi vào db"; sửa FR-176 e]: tầng tiền định `khop-cau-tra-loi.ts` khi phân loại `lech` phải cố NHẬN RA khách đang nói fact nào (mở rộng `chuyenSang` từ một ca `mat_tien` ra mọi khoá trong `TU_KHOA`/`HOI_SO`) và ghi vào đúng fact đó; không nhận ra thì ghi nguyên văn vào fact `bo_sung` (nguồn `seller_chat`, không đóng câu hỏi gốc); câu hỏi gốc vẫn treo và bot hỏi lại bằng lời khác. Chỉ `ack`/`xung_ho`/`hoi` là không ghi. (d) **Điểm đầy đủ tin 0–100, 7 tiêu chí** [nguồn: Gemini lượt 4]: vị trí & hẻm 15 · thông số diện tích 20 · kết cấu & công năng 15 · pháp lý 10 · giá 10 · gợi ý nhu cầu (tiềm năng ở / CHDV / kinh doanh) 20 · lời gọi hành động 10. Tính TIỀN ĐỊNH từ cột `listings` + fact (không tốn model), nói ra với chủ nhà cùng bản nháp ("tin mình 85/100, thêm ảnh sổ là đủ"); **tin ≥ 70 mới được rao trong hệ thống** *[giả định BA: ngưỡng — OPEN-50]* — thay điều kiện `listings_autopublish` hiện tại; đây là điểm TIN, khác hạng NGƯỜI RAO FR-155. Phần thưởng trong kịch bản (push top, broadcast 500 khách VIP) là cho môi giới người; bên mình tương đương là FR-64 báo khách khớp tiêu chí ngay khi tin đủ điểm. Đã code 07/09 (`20260907h`, `khop-cau-tra-loi.ts`, `chat-reply`): bản nháp tin là chữ TIỀN ĐỊNH (không model — số liệu phải đúng từng chữ), gật nhận bằng `laDongY()`; ngưỡng 70 chỉ áp cho tin tạo từ chat (`listings.can_chu_duyet`), tin Excel/admin giữ luật cũ giá+diện tích+phường *[giả định BA]*. Còn thiếu: cron `ask-seller` chưa tự gửi bản nháp khi chủ trả lời câu cuối lúc CTV đang cầm cuộc — chat-reply chỉ gửi trong lượt trả lời. (f) **Điểm tăng dần qua nhiều ngày, không phải một lần** [nguồn: chủ dự án 09/09/2026 — "điểm tăng lên đạt 100% không phải 1 lần mà có thể trong 1 vài ngày khách và bot giao lưu"; chốt "theo khuyến nghị" cùng ngày]: chủ gật bản nháp → tin lên kệ NGAY với điểm hiện có, bong bóng chúc mừng đọc điểm ("chúc mừng anh, tin mình 77/100") và nói cách thêm điểm (2 thứ còn thiếu, "gửi thêm ảnh là điểm tăng ngay"), hứa hỏi thêm vài câu trong mấy ngày tới; **5 phút sau** cron `seller-hoi-bu-tick` hỏi bù câu đầu tiên (`seller_hoi_bu_tick`), các câu sau đi nhịp drip 30 phút (`seller_drip_tick` nay nhận cả tin `dang_ban` tạo từ chat, trần cũ 3 câu/24h/tin, 2 căn/người); tin Excel/admin để yên (OPEN-50 (1)). **Ảnh vào thang điểm**: tiềm năng 20 → 10 (nêu rõ 10 / suy được 5), ảnh 10 đếm TẤM không đếm loại — 1/2/≥3 tấm = 4/7/10, "có 3 ảnh thì tính đủ, ảnh nào cũng được" [chủ dự án 09/09]. Kênh bridge: `ask-seller` không có OA thì xếp câu hỏi vào `reminders` (ghi chú "💬 …", `escalationText` gửi nguyên văn) — trước đó câu hỏi drip trên bridge chưa từng tới chủ nhà. (g) **"Đủ rồi" là chủ nhà nói, không phải điểm nói** [nguồn: chủ dự án 09/09 — "điểm đầy đủ thì chỉ khách nói là đã đầy đủ tin rồi"]: `laDuRoi()` (tiền định) bắt "đủ rồi / vậy thôi / đừng hỏi nữa" → `listings.chu_noi_du_at`, đóng câu treo, vòng hỏi bù DỪNG; **điểm giữ nguyên theo dữ liệu thật, không nhảy lên 100**; "đủ rồi, đăng đi" lúc duyệt = gật + đủ. (h) **Bóc được gì thì lưu ngay, dạng JSON** [nguồn: chủ dự án 09/09 — "bóc tách được gì thì lưu vào backup trước, cột null không quan trọng thì không cần liệt kê"]: `listings.boc_tach` (jsonb, `jsonb_strip_nulls`) ghi lúc tạo tin (`ghi_boc_tach`: loại giao dịch, quận, phường, giá thô, diện tích, phòng ngủ, gấp, dự án) và mỗi fact chủ nhà trả lời (trigger `trg_zz_fact_vao_boc_tach`, kể cả `bo_sung`, `danh_gia`). **Gửi lại chủ nhà ngay** [chủ dự án 09/09 — "đã bóc tách được cái gì, viết gửi lại cho khách luôn"]: bong bóng tiền định "📝 Em ghi nhận: bán nhà phố · hẻm Trần Bình Trọng · Phường 4, Quận 5 · 60m2 · giá 5 tỷ 8 · cần gấp. Sai chỗ nào anh nhắn lại giúp em nha." đứng trước lời chào/câu hỏi đầu; chỉ liệt kê thứ có, sai thì chủ sửa qua FR-164. Cột riêng cho thứ chủ dự án gọi tên: loại giao dịch = `deal` (có sẵn), vị trí = `location_raw`/`ward`/`district` (có sẵn) + fact mới **`vi_tri`** (vị trí cụ thể: đường, số nhà, hẻm — nhóm CƠ BẢN ngay sau phường, mọi loại BĐS, → `location_raw`; câu rao đã nói đường/hẻm thì chat-reply ghi fact ngay, không hỏi lại), **`gap`** (true/false/null, `laGap()`). **Về "định giá"**: sáng 09/09 chủ dự án bỏ "để em xem giá khu đó trước" ("chỉ cần rao và hỏi vị trí cụ thể"); chiều 09/09, đối chiếu chat Gemini 21/06, chủ dự án chốt LẠI: câu hỏi địa chỉ nêu lý do **"để em kiểm tra giá thị trường khu vực"** (`CAU_HOI_MAU.vi_tri`, `SELLER_FEWSHOT`, `SELLER_SCRIPT_RULES`) — đó chỉ là lý do để hỏi, bot vẫn KHÔNG tự đưa con số định giá (TONE "không bịa giá"; so giá chỉ khi khách hỏi, FR-99). `so.ro_hang` thêm cột `gap` và `boc_tach` (`20260909d`, đọc JSON bóc tách ngay trong schema `so`, không phải đổi sang `public.listings`). (i) **Giọng và kết thúc** [nguồn: chủ dự án 09/09 — "hiểu ngữ cảnh căn nhà, lễ phép, không phải 100% nịnh; cuối cùng hỏi chị đánh giá cách em chăm sóc mấy điểm, có mất thời gian không; cần xuống dòng thì xuống dòng"]: `SELLER_SCRIPT_RULES` thêm 3 luật (hiểu ngữ cảnh căn trước khi nói, chỉ khen khi có điểm thật; mỗi ý một dòng; kết thúc xin chấm điểm); cuối vòng hỏi (chủ nói đủ rồi, hoặc tin đã đăng và hết thứ để hỏi) hệ thống mở câu chờ `danh_gia` ("giống người thật không, mất thời gian không, mấy điểm trên 10"), câu trả lời → fact `danh_gia` + `boc_tach`, cảm ơn tiền định, không hỏi lại điểm. Đã code 09/09 (`20260909a`, `khop-cau-tra-loi.ts`, `chat-reply`, `ask-seller`, `tin_nhac.ts`, `prompts.ts` + `bot_prompts`). **(o) Địa chỉ CÓ SỐ NHÀ phải vào tin ngay** [nguồn: chủ dự án 10/09/2026 ~09:50 đọc lại đoạn chat lần 7 — "nó rep vẫn chưa đúng theo instructor lắm"]: mẫu bóc `vi_tri` lúc tạo tin nhận thêm "hẻm 100 Nguyễn Trãi" (số nhà sau chữ hẻm/đường) và "7 Hồng Bàng phường 12" (số nhà trần, chỉ nhận khi ngay sau là phường/quận để "5 tỷ"/"40m2" không thành địa chỉ); mô tả đường vẫn bị loại theo chữ ĐẦU sau hẻm/đường kể cả khi có bề rộng ("hẻm 3m xe máy", "đường 12m vào được container", "hẻm thông không ngập"). Trước bản này hai hình đó rơi, nên bot hỏi lại địa chỉ ba lượt liền dù chủ nhà nói ngay câu đầu, bản nháp không có địa chỉ, và danh sách "đã bán căn nào" chỉ hiện phường nên chủ trả lời "căn Nguyễn Trãi" không khớp được. **(p) Lời ghi nhận không máy móc**: trường còn TRỐNG thì nói "em ghi rồi", chỉ trường đã có giá trị mới nói "em cập nhật lại"; đã có bong bóng ghi nhận thì bong bóng kế vào thẳng câu hỏi (câu mẫu bỏ tiền tố "Dạ em ghi rồi ạ", prompt model được dặn không cảm ơn lần hai); nhãn trong bản nháp không dán lên câu trả lời đã chứa chữ của nhãn (so theo TỪNG CHỮ — "phí QL" + "phí quản lý 20 nghìn/m2" không thành "phí QL phí quản lý…"). Kiểm: e2e N25–N28. Chưa làm: web chưa hiện nhãn "gấp"; ảnh chưa kiểm có đúng căn không (OPEN-53). | M | sếp qua chủ dự án 07/09/2026; chủ dự án 09/09/2026; Gemini "Kịch bản huấn luyện môi giới BĐS" |
| FR-178 | ✅ **Giọng bot xây từ Aioinhadat lên, không đọc mã tin cho khách, có few-shot người bán.** [nguồn: chủ dự án 07/09/2026 tối — "bỏ đọc mã tin, làm few-shot, còn nhiều ý cũ nhadat.cc, muốn chat tự nhiên như con Aioinhadat, build prompt từ bên đó lên, ưu tiên chat Gemini của sếp"; `AOND req + chat examples.docx` §I–II + transcript 30 câu; `Kịch bản huấn luyện môi giới (Gemini 07-09-2026).md` lượt 3]. (a) **Không đọc mã tin (#BDS-…) trong bất kỳ tin nào gửi khách** — mua lẫn bán, kể cả người rao nhiều căn (thay FR-157 c và FR-176 c: neo căn bằng ĐỊA CHỈ/đặc điểm; hết địa chỉ thì phường). Mã vẫn lưu DB, vẫn hiện trên web (UI-C04) và trong tin cho CTV/admin (FR-173 d), vẫn nằm trong khối KHO nội bộ để model điền `send_photos`/`ask_owner`/`viewing` — chỉ cấm viết ra `replies`. (b) **`TONE_RULES` viết lại theo SRD Aioinhadat §II**: mỗi tin dưới 30 từ; khen điểm mạnh thật gắn với khách mua rồi hỏi đúng một thứ; không form, không đọc tên trường; trung thực "hình như là…" với ảnh; giữ các cấm dấu hiệu máy của FR-138. `HUMAN_CHAT_RULES` (mua) giữ luật nghiệp vụ, bỏ mẫu có mã. (c) **`SELLER_SCRIPT_RULES` viết lại** theo (b) + FR-177: cơ bản trước, câu kế nối chi tiết vừa nghe, hệ thống tự ghi thông số lệch, bản nháp/điểm do hệ thống gửi (model không tự soạn). (d) **Few-shot người bán `SELLER_FEWSHOT`** (khoá `bot_prompts.seller_fewshot`): 16 cặp "chủ nói → Thái đáp" chép từ kịch bản Gemini lượt 3 và transcript Aioinhadat, cộng 4 mẫu giọng SAI (đọc tên trường, câu sáo, đọc mã, hỏi dồn); đi vào system prompt nhánh bán cùng luật. (e) **Câu mẫu khi model hỏng là câu người nói** (`CAU_HOI_MAU`/`cauHoiMau()`, một câu cho mỗi fact, có chỗ điền cách gọi) và cũng là "câu gợi ý" đưa cho model — thay "cho em xin thêm kết cấu (số tầng, phòng)" mà sếp chê 07/09. **09/09/2026: sửa được ở Dashboard** [chủ dự án — "thêm cau_hoi_mau vào bot_prompts"]: `bot_prompts.cau_hoi_mau` = JSON {fact_key: câu} (`CAU_HOI_MAU_TEXT`, md5 khớp), bản DB đè từng khoá lên code (`docCauHoiMau`), JSON hỏng thì dùng code + ghi sổ. **Lời chào khách mới** (FR-161) cũng ở `bot_prompts.loi_chao` (`LOI_CHAO`): "…Anh/chị đang muốn mua, thuê hay đang có nhà cần bán/cho thuê ạ? Bên em có anh Thu phụ trách khu vực Sài Gòn, sẽ theo anh/chị tới khi bán được, cho thuê được hay mua được nhà nha." [chủ dự án 09/09 — "anh có manager, anh có Thu ở khu vực Sài Gòn sẽ hỗ trợ anh đến khi bán được nhà hoặc cho thuê hoặc mua được nhà"]; đổi tên người phụ trách thì sửa ở Dashboard. Chưa làm: mặt MUA vẫn theo `chats w B.docx` (nhadat.cc 2024) vì OPEN-48 chưa chốt bỏ mảng nào. | M | chủ dự án 07/09/2026; AOND SRD; Gemini |
| FR-179 | ✅ **Chế độ test: nhắn "hello" là xoá sạch dữ liệu của số Zalo đó, tiếp đón như khách mới.** [nguồn: chủ dự án 09/09/2026 — "bây giờ đang test, mỗi lần bắt gặp khách nói hello thì xoá dữ liệu cũ của người bán này đi, đối xử như người khách mới"]. Công tắc `app_config.test_reset_hello` (1 = bật; **chạy thật phải đặt 0** — khách thật chào "hello" mà mất tin rao là sự cố). Hàm `reset_nguoi_test(p_zalo)` (service_role) xoá theo đúng thứ tự khoá ngoại: deals/viewings/listing_views/info_requests → messages → conversations → listings (cascade fact, ảnh, sự kiện, quan tâm, nhắc) → sellers → buyers → chat_quota; KHÔNG đụng sổ chống trùng, sổ lỗi, CTV, admin. chat-reply gọi sau cổng bí mật (người lạ không xoá được ai), đọc công tắc mỗi lượt không cache, gửi kèm câu "(TEST) Em đã xoá dữ liệu cũ của mình: N tin, M tin nhắn" rồi xử lý "hello" như khách mới. Kiểm: e2e T1/T1b/T2. | C | chủ dự án 09/09/2026 |
| FR-180 | ✅ **Mẫu câu chuẩn — tài sản huấn luyện giọng bot.** [nguồn: chủ dự án 09/09/2026 — "log mọi lượt chat thật, mỗi ngày anh hoặc sếp sửa tay câu bot thành câu mong muốn; lưu thành cặp ngữ cảnh → câu chuẩn; dùng cho cả ví dụ mẫu trong prompt lẫn fine-tune sau; đủ 300 mẫu thì thử Qwen2.5-3B/7B bằng Unsloth trên 4060, ưng thì Gemini Flash tuning hoặc VPS GPU"]. (a) Log chat thật đã có (`messages`, `so.hoi_thoai`). (b) Bảng `mau_cau` (`20260909c`): câu bot thật (`message_id`, FK set null để mẫu sống lâu hơn hội thoại) + `ngu_canh` 8 lượt trước (RPC `ngu_canh_tin`) + `cau_bot` + `cau_chuan` + `dung_lam` (vi_du / fine_tune / ca_hai / bo) + người sửa; RLS `la_admin()`, anon không thấy; `so.mau_cau` để đọc. (c) Trang `/admin/mau-cau`: hội thoại mới nhất → từng lượt → "Sửa thành câu chuẩn" ở mỗi câu bot, thanh tiến độ /300 theo phía, tải JSONL. (d) **Bot dùng ngay**: `mau_cau_fewshot(phia, 12)` nối sau few-shot soạn tay trong system prompt cả hai phía (cache 60 s cùng `napCauHinh`) — mẫu sửa hôm nay, một phút sau bot bắt chước. (e) **Fine-tune**: `scripts/xuat-mau-cau.mjs` → `mau-cau.sharegpt.jsonl` (Unsloth, system = `tone_rules` đang chạy) + `mau-cau.gemini.jsonl`; `train/unsloth_qwen.py` LoRA Qwen2.5-3B 4-bit trên RTX 4060 (WSL2), giữ 10 % kiểm, sinh 20 câu thử `thu.txt`, xuất GGUF; `train/README.md` lộ trình 3 bước. Ngưỡng "ưng" chưa chốt bằng số (OPEN-54). | M | chủ dự án 09/09/2026 |
| FR-181 | ✅ **Mỗi khách một tên trợ lý riêng, giữ suốt; không còn một tên Thái.** [nguồn: chủ dự án 09/09/2026 chiều (đối chiếu chat Gemini "AI Ơi Nhà Đất" 21–27/06/2026, chốt từng dòng OPEN-55) — "Mỗi khách một tên T•ai, Kh•ai…"; chat 21/06 §II "Mỗi khách hàng tiếp cận có 1 nhân viên H•ai, T•ai, L•ai", "xài hết phụ âm tiếng Việt", "viết hoa chữ đầu", "T có t•ai và tr•ai", "K chỉ có kh•ai"… chủ dự án giữ cả K•ai; "P chỉ có ph•ai"; đảo lại OPEN-39 03/09]. Kho 20 tên `KHO_TEN_TRO_LY` (B•ai C•ai D•ai Đ•ai G•ai Gi•ai H•ai K•ai Kh•ai L•ai M•ai N•ai Nh•ai Ph•ai Q•ai R•ai S•ai T•ai Tr•ai V•ai — không P•ai). Gán MỘT lần theo Zalo ID bằng băm tất định (`tenTroLy()`, FNV-1a), không random — hôm nay hay tháng sau cùng một người gặp cùng một tên; một người vừa bán vừa mua gặp cùng tên ở cả hai nhánh (một trợ lý xuyên suốt — chat 21/06 "chỉ 1 trợ lý nói chuyện xuyên suốt với 1 người rao"). Lưu `sellers.ten_tro_ly` (lượt đầu) và `buyers.preferences.ten_tro_ly` (đi chung RPC gộp hồ sơ). `TONE_RULES`, `LOI_CHAO`, `SELLER_FEWSHOT` viết "{ten}", `dienTen()` điền ở chat-reply/ask-seller/nudge; `bot_prompts` cùng chữ (`20260909h`); `mau_cau_fewshot` ghi "→ Trợ lý:" thay "→ Thái:" (mẫu dạy giọng, không dạy tên). CRM: `seller_ranks.ten_tro_ly`, `so.nguoi_ban.tro_ly`. Không nhận: bot mua và bot bán là hai người khác nhau (M•ai/T•ai) như chat — hệ thống một `chat-reply` tách vai từng lượt (FR-157), đổi tên giữa chừng là điều chính chat 21/06 cấm. | M | chủ dự án 09/09/2026 chiều (đối chiếu chat Gemini "AI Ơi Nhà Đất" 21–27/06/2026, chốt từng dòng OPEN-55) |
| FR-182 | ✅ **Chat Gemini 21/06 đối chiếu với hệ thống — 14 dòng đã chốt từng dòng.** [nguồn: chủ dự án 09/09/2026 chiều (đối chiếu chat Gemini "AI Ơi Nhà Đất" 21–27/06/2026, chốt từng dòng OPEN-55)] Bảng quyết định nằm ở OPEN-55 (giữ nguyên: Zalo cá nhân qua bridge chỉ gửi chữ; loại BĐS trước rồi vị trí linh hoạt; hỏi đúng MỘT thông tin; gán nhãn tự động không nêu phí; lên kệ ≥70 điểm/ảnh 10/sổ không bắt buộc; nhịp hỏi bù 5 phút–30 phút–3 câu/ngày, không luật 7 ngày Zalo; domain aioinhadat.vercel.app; Supabase không SharePoint. Đổi theo chat: FR-181 tên theo khách; FR-186 hướng cho chung cư/đất; FR-173 a chủ nhà trước 12 giờ; FR-177 h "kiểm tra giá thị trường khu vực"; FR-183 điểm người rao; FR-114 d kiến thức dự án; FR-184 bán rồi; FR-185 ảnh vào kho + phân loại + OCR). Không phải tính năng — là mốc truy vết để sau này ai đọc chat Gemini không tưởng hệ thống đang lệch. | S | chủ dự án 09/09/2026 chiều (đối chiếu chat Gemini "AI Ơi Nhà Đất" 21–27/06/2026, chốt từng dòng OPEN-55) |
| FR-183 | ✅ **Điểm người rao 0–100, song song với điểm từng tin.** [nguồn: chủ dự án 09/09/2026 chiều (đối chiếu chat Gemini "AI Ơi Nhà Đất" 21–27/06/2026, chốt từng dòng OPEN-55) — "có cả điểm từng tin và điểm người rao"; chat 21/06 §IV: "Điểm của họ là trung bình điểm của các BĐS họ đang rao… tăng nhanh trong 10 BĐS 5–7%, sau đó 3–5%, sau 30 BĐS 1–2%"]. `diem_nguoi_ban(seller_id)` (`20260909h`): trung bình `diem_tin` các tin đang rao (`cho_thong_tin`/`dang_ban`/`dang_quan_tam`) × hệ số quy mô — NMG: +6%/căn tới 10 căn, +4%/căn 11–30, +1,5%/căn sau 30 (lấy giữa các khoảng chủ dự án nêu); CCRB ×1; trần 100; tính TẠI CHỖ, không lưu cột (lý do FR-155). Trả `{diem, diem_tb, so_tin, he_so}`; chỉ service_role/admin. Lộ ở `seller_ranks.diem_nguoi_rao` và `so.nguoi_ban.diem_nguoi_rao`. Bot nói ra điểm này trong bong bóng chúc mừng lên web khi người rao có ≥2 căn ("Điểm người rao của anh hiện 74/100 (3 căn đang rao); tin nào đủ hơn là điểm chung lên theo") — một căn thì điểm người = điểm tin, không nói hai lần. Chưa: "BĐS mới lấy điểm người rao làm điểm ban đầu" (chat §IV) — trái nguyên tắc điểm tin tiền định theo dữ liệu (FR-177 d), không nhận; hạng Đồng/Bạc/Vàng theo điểm này — OPEN-26; trang CRM web chưa hiện cột (view có). | M | chủ dự án 09/09/2026 chiều (đối chiếu chat Gemini "AI Ơi Nhà Đất" 21–27/06/2026, chốt từng dòng OPEN-55) |
| FR-184 | ✅ **Chủ nhà báo "bán rồi / không bán nữa" thì bot tự đóng tin và xác nhận; nhiều căn thì hỏi căn nào.** [nguồn: chủ dự án 09/09/2026 chiều (đối chiếu chat Gemini "AI Ơi Nhà Đất" 21–27/06/2026, chốt từng dòng OPEN-55) — "làm Người bán tự báo bán rồi thì bot đóng tin và xác nhận, nhiều căn đang mở thì hỏi đóng căn nào"; chat 21/06 lượt 2 + transcript câu 35–38; UF-13]. Tiền định `laNgungRao()` (`khop-cau-tra-loi.ts`): "bán rồi / đã bán / có người thuê rồi / nhận cọc rồi / chốt rồi" → `ban_roi`; "ngưng bán / không bán nữa / rút tin / gỡ tin / để lại ở" → `rut`; câu hỏi ("bán rồi hả em?"), phủ định ("chưa bán", "vẫn đang bán"), câu rao/giá ("bán 5 tỷ", "chốt giá 5 tỷ") → không. Đang DUYỆT BẢN NHÁP mà nói "chốt đi / ok đăng đi" là GẬT (FR-177 c), không phải báo bán. Một căn đang rao → `listings.status` = `da_chot` (bán rồi; trigger FR-108 báo khách đang chờ) hoặc `an` (rút), `chu_noi_du_at` đóng dấu, mọi `info_requests` pending của căn → `expired`, `reminders` pending của căn → `cancelled`, `boc_tach.ket_thuc`/`ket_thuc_luc`/`ket_thuc_loi`; bong bóng tiền định: "Chúc mừng anh đã bán được căn …! Em đã gỡ tin khỏi kệ và báo các khách đang chờ" / "Em đã ngưng rao căn … theo ý anh". Nhiều căn (`cho_thong_tin`/`dang_ban`/`dang_quan_tam`) → liệt kê "1. địa chỉ · 2. …", mở câu chờ `ngung_rao_can_nao` (answer = kiểu), lượt sau `chonCanTheoCau()` đọc số thứ tự / "cái đầu" / địa chỉ / phường / mã; không rõ thì hỏi lại; không tốn model. Không có căn nào → nói thật. | M | chủ dự án 09/09/2026 chiều (đối chiếu chat Gemini "AI Ơi Nhà Đất" 21–27/06/2026, chốt từng dòng OPEN-55) |
| FR-185 | ✅ **Ảnh chủ nhà gửi qua chat vào KHO của mình, được phân loại, ảnh sổ đọc diện tích đối chiếu.** [nguồn: chủ dự án 09/09/2026 chiều (đối chiếu chat Gemini "AI Ơi Nhà Đất" 21–27/06/2026, chốt từng dòng OPEN-55) — "thì ảnh phải gắn vào kho chứ", "phân loại ảnh (mặt tiền, nội thất, giấy tờ) và OCR sổ đối chiếu diện tích"; chat 21/06 lượt 7; OPEN-32 đóng]. Nhánh bán `nhanAnh()`: (1) `_shared/ai/phan-loai-anh.ts` hỏi model một lượt (`effort: low`, schema `AnhSchema`): `loai` ∈ mat_tien / trong_nha / hem / giay_to / ban_ve / khac, `mo_ta` ≤15 từ "hình như…", với giấy tờ `{loai_giay, dien_tich_m2, dia_chi, so_thua, so_to, ro_net}` — TUYỆT ĐỐI không đọc tên người/CCCD (§5); (2) `_shared/kho_anh.ts`: `taiAnh()` (15 s, ≤8 MB, chỉ mime ảnh) → `catAnhVaoKho()`: ảnh nhà → `listing-public`, giấy tờ → `listing-private`, KHÔNG phân loại được → `listing-private` (`media_type='giay_to'` để CHECK bucket bắt), `listing_media` thêm `nguon='seller_chat'`, `mo_ta`, `ocr` (`20260909h`); `sort_order` = số dòng hiện có (luật 0..9999 của `20260907a`); insert hụt thì gỡ file. Không còn fact `hinh_anh` URL Zalo khi đã vào kho (đếm điểm ảnh qua `listing_media`, FR-177 f); kho hỏng → rơi về fact URL tạm + `bot_errors` "anh vao kho" (ảnh không mất). Câu chờ `hinh_anh` đang treo thì đóng. (3) Giấy tờ: chưa có `area_m2` → fact `dien_tich` nguồn `so_do_ocr` ("Sổ ghi 48.5m2, em lấy số đó"); lệch >5% → hỏi lại "số nào đúng", fact `bo_sung` + việc 📐 cho admin; khớp → nói khớp; địa chỉ trên sổ → fact `bo_sung`. Bong bóng nhận ảnh nói loại ảnh + lợi ích cho khách, giấy tờ thì "em cất riêng, không đưa lên web". Ảnh gửi trần → chỉ nhận ảnh, không coi chuỗi rỗng là câu trả lời. Ảnh KHÁCH MUA gửi vẫn đi FR-134 (đọc, không cất). Chưa: kiểm ảnh có đúng căn không (OPEN-53); phân loại ảnh đã có trong kho từ trước. | M | chủ dự án 09/09/2026 chiều (đối chiếu chat Gemini "AI Ơi Nhà Đất" 21–27/06/2026, chốt từng dòng OPEN-55) |
| FR-186 | ✅ **Bộ câu hỏi theo loại BĐS và cho thuê, thứ tự hỏi giống người trong nghề; hướng hỏi với chung cư và đất.** [nguồn: chủ dự án 09/09/2026 chiều (đối chiếu chat Gemini "AI Ơi Nhà Đất" 21–27/06/2026, chốt từng dòng OPEN-55) — "Hỏi hướng chung cư, hướng đất", "Bộ câu hỏi riêng cho chung cư, đất, biệt thự, cho thuê (HĐMB, nội thất để lại, cột điện hố ga, cọc, trượt giá)", "đưa lại thứ tự hỏi chuẩn giống người cho các loại bds khác nhau"; chat 21/06 lượt 21 + §III]. `required_facts` thêm `deal` (null = mọi giao dịch, `cho_thue` = chỉ tin thuê), reseed toàn bảng (`20260909h`), khoá mới (property_type, fact_key, deal): NHÀ PHỐ vị trí → phường → diện tích → giá │ hẻm → lầu → phòng → pháp lý → **tiềm năng** (mới, câu "hợp để ở hay kinh doanh ngành gì") → ảnh │ phụ: hướng, quy hoạch, năm xây (không hỏi); NHÀ CẤP 4 tương tự với hiện trạng; CHUNG CƯ dự án/toà → phường → m2 → giá │ tầng → phòng → **hướng ban công** → nội thất bàn giao (nhà trống/để lại gì) → **sổ hồng hay HĐMB** → phí QL → ảnh; ĐẤT vị trí → phường → diện tích → thổ cư → giá │ đường → **hướng** → **hạ tầng** (`ha_tang`: cột điện, hố ga, đường đâm) → **xây tự do / theo mẫu CĐT** (`xay_dung`) → sổ riêng hay đất dự án → ảnh; BIỆT THỰ kết cấu → phòng → sân vườn/ô tô → hẻm → **khu biệt lập** (`khu_compound`) → pháp lý hoàn công → ảnh; CHO THUÊ (nhà phố/cấp 4/chung cư/biệt thự) thêm nội thất để lại → **cọc** (`tien_coc`) → thời hạn thuê → **trượt giá** (`truot_gia`); phòng trọ/mặt bằng có cọc (+ trượt giá mặt bằng). View `listing_missing_facts` lọc `deal`. Câu mẫu riêng theo loại: khoá `fact@loai` trong `CAU_HOI_MAU` (`huong@chung_cu` "Ban công căn mình quay hướng nào", `huong@dat`, `vi_tri@chung_cu`, `phap_ly@chung_cu` "đã ra sổ hồng chưa hay còn hợp đồng mua bán", `phap_ly@dat`, `noi_that@chung_cu`…), `cauHoiMau(key, ac, bang, loai)`; `FACT_LABELS`/`NHAN_HOI_LAI`/`nhan_fact()` đủ khoá mới; `khop-cau-tra-loi.ts`: `HOI_SO` + `tien_coc`/`truot_gia`, `TU_KHOA` + `ha_tang`/`xay_dung`/`khu_compound`, `nhanDienFact` nhận "cọc 2 tháng", "tăng 5%/năm", "cột điện", "xây tự do", "compound", "bàn giao nhà trống"; `LIEN_QUAN` nối chuỗi mới (tầng → phòng → hướng → nội thất → pháp lý; đường → hướng → hạ tầng → xây dựng → pháp lý; pháp lý → cọc → thời hạn → trượt giá). `SELLER_SCRIPT_RULES` viết lại đoạn thứ tự theo loại. Soát 09/09: seed `required_facts` cho các loại ngoài nhà phố VẪN CÓ trong DB (74 dòng) — báo cáo trước đó nói "mất theo OPEN-46" là sai; nay seed lại toàn bộ trong một file nên không còn phụ thuộc 41 migration mất. Mock e2e chép đúng bảng mới. **Mở rộng 09/09 tối (`20260909i`, đọc lại hai chat Gemini để không bỏ sót loại nào):** (1) 4 loại mới trong enum `property_type` — `toa_nha` (CHDV / khách sạn / toà nhà dòng tiền: số phòng → tỷ lệ lấp đầy → doanh thu → kết cấu → thang máy → PCCC → pháp lý → hẻm), `dat_nong_nghiep` (quy hoạch → lên thổ cư → đường vào xe tải → nguồn nước → ranh giới → pháp lý), `dat_kinh_doanh` (SKC/TMD: thời hạn sử dụng → trả tiền thuê đất một lần/hàng năm → mục đích → đường → pháp lý), `kho_xuong` (chiều cao thông thủy → tải trọng sàn → trạm biến áp → nước thải → xe container → pháp lý → thời hạn; cho thuê thêm cọc/thời hạn/trượt giá/fit-out); `guess_property_type`/`_answer` nhận từ khoá mới (toà nhà xét TRƯỚC chung cư vì "căn hộ dịch vụ"); `diem_tin` có nhánh kết cấu riêng (phòng + doanh thu; chiều cao + tải trọng; quy hoạch/thời hạn); web `TYPE_LABEL`/bộ lọc/tag (`toa-nha`, `kho-xuong`; đất nông nghiệp + kinh doanh gộp tag `dat`)/`parse-query`; form admin sửa tin lấy loại từ `TYPE_LABEL` (bản cũ chép tay có `can_ho` không tồn tại trong enum). (2) **Nhóm `sau_dang`** (priority 30+) — câu người trong nghề hỏi SAU khi tin đã lên kệ, đi bằng vòng hỏi bù 30 phút: nhà phố/cấp 4/biệt thự `so_wc`, `cach_mat_tien`, `hem_thong`, `ngap_nuoc`, `hien_trang_su_dung` (đang ở/cho thuê/trống), `the_chap` (sổ cầm tay hay ngân hàng), `tien_ich_gan`, `ly_do_ban`, `thuong_luong`; chung cư `view`, `can_goc`, `phi_gui_xe`, `so_huu` (lâu dài/50 năm); đất `hinh_dang`, `mat_do_xd`, `tang_cao_toi_da`; cho thuê `fit_out`. `chat-reply` lọc `nhom !== "sau_dang"` ở cả câu đầu và câu kế — bản nháp chỉ chờ `co_ban` + `chuyen_mon`, chủ nhà không bị hỏi 15 câu mới thấy tin (bài học `phu` 07/09: ngưỡng ưu tiên không phải cấm, phải LỌC). View `listing_missing_facts` coi "đã có" thêm `bathrooms`, `rear_width_m`, `distance_to_street_m`, `corner_lot`, `has_elevator`, `negotiable`, `rent_income_vnd`. `khop-cau-tra-loi.ts`: `HOI_CO_KHONG` (câu có/không: "có", "không", "cụt", "thông", "cầm tay"… là đáp án, không phải ack), `HOI_SO` + 13 khoá số, `nhanDienFact` nhận "2 WC", "cách mặt tiền 50m", "nở hậu 1m", "không ngập", "hẻm cụt", "đang thế chấp", "còn thương lượng", "đang cho thuê", "lý do định cư", "gần trường", "căn góc", "thang máy", "PCCC", "lấp đầy 90%", "cao thông thủy 9m", "tải trọng 2 tấn", "250 kVA", "container", "lên thổ cư", "cắm cọc", "trả tiền thuê đất hàng năm", "sở hữu 50 năm", "mật độ xây dựng", "bóp hậu", "xây tối đa 5 tầng", "fit-out 15 ngày". `CAU_HOI_MAU`/`FACT_LABELS`/`nhan_fact()` đủ 36 khoá mới; `SELLER_SCRIPT_RULES` thêm đoạn thứ tự 4 loại mới + câu hỏi bù sau đăng. Kiểm: e2e N13/N13b (kho xưởng), N14 (sau_dang có trong view nhưng KHÔNG hỏi trước nháp). **(o) Không hỏi lần thứ hai** [nguồn: chủ dự án 10/09/2026 00:40 ("triển khai hết không được sót cái nào"; chat Gemini 21/06 lượt 1, 33, 34, 35–36, 57–60); chat 21/06 lượt 1]: câu chủ nhà đã né (info_request `expired`) thì cả vòng hỏi trong chat lẫn `ask-seller` hỏi bù và keep-alive đều bỏ qua, không hỏi lại. **(q) Rao theo LÔ** (chân dung đại diện chủ đầu tư, 10/09 lần 6): một tin "căn A5 8x20 giá 18 tỷ, căn A7 …, căn B2 góc 10x20 giá 22 tỷ" → `nhanDienNhieuCan` tách từng căn, mỗi căn một tin riêng (`unit_code`, ngang×dài, giá; kế thừa dự án/quận/phường/loại của căn đang nói), hỏi MỘT câu chung cho lô; tin trong dự án lấy quận/phường của dự án khi câu rao không nói. **(r) Nhà đầu tư kể "mua nhà cũ sửa lại bán, có căn … 5 tỷ 9"** là người bán (`coHangCoGia`: có hàng + có giá/m² thắng ý định mua). **(s) Câu rao dài** bóc hết ngay lúc tạo tin (FR-177 n). **(p) Địa bàn Vũng Tàu** (sáp nhập TP.HCM 07/2025, chân dung căn hộ nghỉ dưỡng): `dia_ban.ts` nhận Vũng Tàu, Bà Rịa, Phú Mỹ, Long Hải, Hồ Tràm. | M | chủ dự án 09/09/2026 chiều (đối chiếu chat Gemini "AI Ơi Nhà Đất" 21–27/06/2026, chốt từng dòng OPEN-55); tối: chủ dự án 09/09/2026 tối ("chia luôn từ lúc có câu rao phân loại… file json dành cho từng mã BĐS chứ không phải nguyên một rổ dài… trang null trong Supabase thì không cần hiển thị… chia nhóm luôn, đọc lại 2 cái chat Gemini xem có trường hợp nào, đây là production nên không thể bỏ sót"); chat Gemini 21/06 lượt 38 (§III SRD: toà nhà dòng tiền, đất nông nghiệp, SKC/TMD, kho xưởng) + lượt 65–67 (câu hỏi bù: móng, hẻm thông, ngập, cách mặt tiền, trường học, công chứng, gym); chat 07/09 (sổ cầm tay hay thế chấp, đang ở hay cho thuê, động cơ bán, giá linh động) |
| FR-188 | ✅ **Cần bán gấp / không gấp là một thông tin, hỏi ngay sau giá; khách mua cũng có cờ gấp; căn gấp lên đầu khi ghép.** [nguồn: chủ dự án 10/09/2026 00:40 ("triển khai hết không được sót cái nào"; chat Gemini 21/06 lượt 1, 33, 34, 35–36, 57–60)] Fact `gap` (`required_facts` nhóm `co_ban` priority 10 — câu cuối nhóm cơ bản, tức ngay sau giá; 11 loại), câu mẫu "Mình cần ra hàng gấp hay được giá thì thôi?"; bắt ở MỌI lượt (`nhanDienFact` → fact `gap`, kể cả "không vội, được giá thì thôi"); trigger `listing_facts_sync_gap` + `doc_gap()` đổ true/false vào `listings.gap` (cùng luật với `laGap()`); `listing_missing_facts` coi đã có khi cột không null; bản nháp ghi "CẦN BÁN GẤP". Người MUA: "cần tìm gấp" → `buyers.preferences.gap` (tiền định); kho gợi ý căn sắp `gap desc` trước `created_at`. **(b) Câu MỀM hỏi đúng MỘT lần** [nguồn: chủ dự án 10/09/2026 00:40 ("không hỏi lần thứ hai"), bắt ở lần chạy thật 7]: `HOI_MOT_LAN` = gap, ly_do_ban, thuong_luong, tiem_nang, muc_dich — đang treo câu đó mà chủ nhà nói thứ khác thì câu `expired` NGAY (luật né-2-lần chỉ áp cho câu cứng: giá, địa chỉ, diện tích, pháp lý); vòng hỏi bù hôm sau vẫn được nhắc lại vì đó là một lượt/ngày gom 2–3 câu. Trước bản này đại diện CĐT bị hỏi "cần bán gấp hay không" ba lượt liền. Kiểm: e2e N18/N18b/N18c/N19/N25. | M | chủ dự án 10/09/2026 00:40 ("triển khai hết không được sót cái nào"; chat Gemini 21/06 lượt 1, 33, 34, 35–36, 57–60) |
| FR-189 | ✅ **Người thật giữ khách / trả lại bot.** [nguồn: chủ dự án 10/09/2026 00:40 ("triển khai hết không được sót cái nào"; chat Gemini 21/06 lượt 1, 33, 34, 35–36, 57–60); chat 21/06 lượt 33 "nhân viên thật theo dõi hội thoại, Takeover/Release"] Cột `conversations.human_hold`; người nội bộ (CTV/admin, `nguoi_noi_bo`) nhắn "#<mã tin> giữ" → bot im vô thời hạn với chủ căn đó (cả nhánh bán lẫn mua qua `ensure_*_conversation` trả `c_human_hold`), "#<mã tin> trả bot" → bot chăm tiếp; `/admin/tin-nhan` có nhãn "người thật giữ" + nút Giữ khách / Trả bot (RPC `giu_khach`, admin). Nhường sân 30 phút sau tin gõ tay (FR-141) vẫn giữ. Kiểm: e2e N20/N20b/N20c. | M | chủ dự án 10/09/2026 00:40 ("triển khai hết không được sót cái nào"; chat Gemini 21/06 lượt 1, 33, 34, 35–36, 57–60) |
| FR-190 | ✅ **CTV theo khu vực, trần 15 ca đang mở.** [nguồn: chủ dự án 10/09/2026 00:40 ("triển khai hết không được sót cái nào"; chat Gemini 21/06 lượt 1, 33, 34, 35–36, 57–60); chat 21/06 lượt 34] `ctvs.khu_vuc text[]` (quận/huyện phụ trách, rỗng = mọi khu), `app_config.ctv_tran_ca` (15), `ctv_dang_ganh()` = câu khách chờ + hội thoại cần người thật, `chon_ctv(district)`: đúng khu → CTV không giới hạn khu → khu khác, bỏ CTV đã đủ trần, trong nhóm chọn ít ca hơn; `route_info_request` (câu khách hỏi khi chủ không có Zalo) và `assign_ctv_round_robin` (hội thoại mới) đều đi qua đó; hết CTV → admin. | M | chủ dự án 10/09/2026 00:40 ("triển khai hết không được sót cái nào"; chat Gemini 21/06 lượt 1, 33, 34, 35–36, 57–60) |
| FR-191 | ✅ **Keep-alive 6 ngày theo luật 7 ngày Zalo.** [nguồn: chủ dự án 10/09/2026 00:40 ("triển khai hết không được sót cái nào"; chat Gemini 21/06 lượt 1, 33, 34, 35–36, 57–60); chat 21/06 lượt 35–36 "thay vì chỉ hỏi còn bán không, nhờ bổ sung 1 thông tin còn thiếu"] Cron `seller-keep-alive-tick` 09:30 VN: tin `dang_ban`, chủ có Zalo, chủ im ≥ 6 ngày, chưa hỏi gì 6 ngày → còn thông tin chưa hỏi (và chưa bị né) thì `ask_seller_drip` (gom 2–3 câu), hết thì hỏi "còn bán không" (`con_ban`). `stale-listing-tick` 30 ngày giữ nguyên. | M | chủ dự án 10/09/2026 00:40 ("triển khai hết không được sót cái nào"; chat Gemini 21/06 lượt 1, 33, 34, 35–36, 57–60) |
| FR-192 | ✅ **Quota tiêu hao hiện trong CRM: lượt gọi model so với trần ngày, số dư tài khoản model, người nhắn nhiều nhất so với trần theo người.** [nguồn: chủ dự án 10/09/2026 ~10:20 ("có chỗ để hiển thị quota tiêu hao trong admin đi")] RPC `quota_tieu_hao()` (`20260910a`, SECURITY DEFINER, tự kiểm `la_admin()` — không mở bảng `chat_quota` cho web vì bảng đó chứa Zalo ID trần của cả người lạ, §5) trả một JSON gồm: (1) `bot_usage.model_calls` hôm nay so với trần `DAILY_MODEL_CALL_CAP` đọc từ Vault (mặc định 1000, đúng con số `chat-reply` dùng) kèm dấu `capped_at`; (2) mười người nhắn nhiều nhất 24 giờ từ `chat_quota` (lượt trong giờ, lượt 24h, có phải người quen không) so với trần `bump_user_quota` 30/giờ · 120/ngày, người quen ×4; (3) **số dư tài khoản Anthropic** — API không cho hỏi số dư, nên đọc dấu vết trong `bot_errors`: lượt gọi bị trả 400 vì hết số dư; (4) tổng chữ-máy hôm nay, quy ra đô bằng bảng giá đã có ở thẻ Chi phí Model. Web: thẻ "Quota tiêu hao" đầu tab **Vận hành** của `/admin`, cộng một băng đỏ trên cùng mọi tab khi hết số dư hoặc chạm trần. Băng đó là điểm chính của yêu cầu: hết số dư thì bot VẪN trả lời bằng câu mẫu tiền định (OPEN-30) nên nhìn hội thoại không thấy khác gì — sáng 10/09 chủ dự án đọc bản ghi lần 8 chỉ thấy "giọng cứng" mà không ai biết model đã chết 48 lượt liền. Kiểm: TS-QUOTA-01…03. **Sửa 10/09 chiều (`20260910l`):** cờ `het_credit` cần CẢ HAI vế — có lỗi trong 24h VÀ chưa có dấu `bot_health(model_chinh)` nào mới hơn lỗi cuối. Chủ dự án nạp tiền lúc trưa, model gọi lại được, mà thẻ vẫn đỏ "HẾT SỐ DƯ" vì 68 dòng lỗi buổi sáng chưa trôi khỏi cửa sổ 24 giờ — "nếu còn tiền mà nó thông báo vậy là sai rồi". Dấu sống do `_shared/claude.ts` đóng khi lượt gọi model chính TRẢ VỀ (tiết chế 2 phút/lượt), cố ý KHÔNG dùng `bot_usage`/`cong_token` vì đường dự phòng Groq cũng ghi token qua đó và sẽ xoá oan cờ. Thẻ nay có BA trạng thái, không phải hai: đang hết · đã hết rồi gọi lại được lúc HH:MM · chưa hề bị từ chối. Kiểm: TS-QUOTA-04. | M | chủ dự án 10/09/2026 ~10:20 ("có chỗ để hiển thị quota tiêu hao trong admin đi") |
| FR-193 | ✅ **Đọc được chữ THIẾU DẤU và viết tắt lẫn trong câu có dấu; không tự khẳng định quận; chào lại khi khách chào.** [nguồn: chủ dự án 10/09/2026 ~10:45, nhắn thật vào Zalo OA: "Chào bạn tôi cần bán căn ho ở Hà đô centrosa garden" → bot đáp "Em ghi nhận: bán · Quận 5" rồi hỏi lại loại BĐS] (a) `guess_property_type` / `guess_property_type_answer` (`20260910b`) bỏ hẳn lối chia hai nhánh theo TOÀN CHUỖI (có dấu / không dấu) — người thật gõ lẫn lộn trong cùng một câu, nên câu trên rơi vào nhánh có dấu và luật "căn hộ" trượt khỏi "căn ho". Nay luôn so trên `bo_dau()`, thêm lối viết tắt `cc`, `ch`, `np`, `bt`, `mb`, `c4`, `dn`. Mock e2e bỏ dấu y hệt (`boDauMock`) — mock lệch bản thật thì bộ e2e đo sai đúng chỗ đó. (b) Địa bàn: cột `listings.district` vẫn NOT NULL mặc định "Quận 5" (di sản thời chỉ làm chợ Quận 5), nhưng LỜI NÓI với khách chỉ nhắc quận khi ĐỌC ĐƯỢC thật — từ câu rao (`bocQuan`) hoặc từ dự án khớp trong kho. Nói "Quận 5" cho một căn ở Quận 10 là bịa, và khách đọc bong bóng đầu tiên sẽ mất tin ngay. (c) Khách mở lời bằng câu chào ("Chào bạn", "hi", "alo") thì bong bóng ghi nhận chào lại trước rồi mới liệt kê — bong bóng đó là tiền định, đi TRƯỚC câu của model, nên trước bản này đoạn mở đầu luôn cụt lủn kể cả khi model còn sống. (d) Còn thiếu, cần dữ liệu chứ không phải luật: tên DỰ ÁN không nhận ra ("Hà Đô Centrosa Garden") vì bảng `projects` mới có 1 dòng, nên `match_projects` trả rỗng và tin không lấy được quận/phường của dự án — xem OPEN-56. **(b2) Quận nói ở LƯỢT SAU cũng vào cột**: `capNhatQuan()` chạy mỗi lượt người bán, thấy quận trong câu thì sửa `listings.district` và ghi vào JSON bóc tách — trước bản này chỉ phường vào cột, nên một căn hộ Quận 10 nằm trong rổ Quận 5. **(c) Tên DỰ ÁN**: `match_projects` (`20260910c`, `20260910d`) so trên chuỗi bỏ dấu, khớp trọn tên hoặc theo TỪ LÕI (≥2 từ, ≥60%) — khách nói "Vinhomes Grand Park" vẫn ra dù kho ghi "Glory Heights - Vinhomes Grand Park", "cát tường phú sinh" ra "Cát Tường Phú Sinh Eco City". **(d) Kho dự án**: `scripts/thu-du-an.mjs` nạp **1.643 dự án** TP.HCM (1.160) · Bình Dương (284) · Long An (199) — dữ kiện (chủ đầu tư, địa chỉ, giá từ, đơn giá m2, tổng diện tích, dải diện tích, pháp lý, năm bàn giao, tiện ích) + `source_url`; mô tả VIẾT LẠI từ dữ kiện, không chép bài của nguồn (nội dung trùng lặp hại SEO của chính Aioinhadat). OPEN-56 khép phần dữ liệu. **(g) Từ đặc trưng phải vừa DÀI vừa HIẾM** (20260910j): ngay lượt bắn tin sau khi nới luật (f), khách nhắn "dự án Lam Sơn Riverside quận 4" thì bot đáp "dự án An Sơn Riverside" — chữ "riverside" có hàng chục dự án cùng dùng. Nay từ đó phải xuất hiện trong nhiều nhất 3 tên dự án của cả kho, tần suất tính ngay trong truy vấn nên tự đúng khi kho lớn lên. Gán nhầm rồi ĐỌC TÊN SAI cho khách nghe còn tệ hơn im. **(f) Một TỪ ĐẶC TRƯNG (≥7 ký tự) là đủ để khớp** (20260910g): khách hỏi "The Beverly có mấy loại căn" thì tên trong kho là "The Beverly - Vinhomes Grand Park", bỏ từ chung còn đúng một từ lõi nên luật ≥3 từ liên tiếp trượt và bot phải nói "em chưa nắm chắc" dù kho có đủ. Ngưỡng 7 chọn theo tiếng Việt đơn âm — "nguyen", "phuong", "truong" đều ≤6 nên tên đường không lọt, còn "beverly", "vinhomes", "centrosa", "masteri" thì lọt. Kiểm: e2e N30/N31 + V1.3 (không tự thêm quận), TS-LOAI-01 (14 câu thật trên DB), TS-DUAN-01. | M | chủ dự án 10/09/2026 ~10:45 (ảnh chụp Zalo OA) |
| FR-194 | ✅ **Đường dự phòng khi model chính hết số dư: tự chuyển sang Groq thay vì rơi về câu mẫu.** [nguồn: chủ dự án 10/09/2026 ~11:10 cấp khoá Groq — "dùng tạm cái key groq, nó ngu hơn ko nhỉ"] Sáng 10/09 tài khoản Anthropic hết số dư, 48 lượt trả 400 liên tiếp, `chat-reply` rơi về CÂU MẪU tiền định (OPEN-30). Bot vẫn trả lời nên nhìn hội thoại không khác gì — không ai biết model đã chết. `_shared/groq.ts` là lớp chuyển đổi: phơi đúng `messages.create` + `messages.parse` rồi dịch sang API giọng OpenAI của Groq, dịch cả `usage` về bốn ô mà `cong_token` ghi sổ. `anthropicClient()` BỌC client chính, nên cả 10 chỗ gọi có lưới mà không sửa chỗ nào; chỉ chen vào khi lỗi thuộc nhóm hết tiền / quá nhịp / quá tải (`nenDoiSang()`), lỗi khác vẫn ném lên như cũ. Khoá và model nằm trong Vault: `GROQ_API_KEY`, `GROQ_MODEL` (mặc định `qwen/qwen3.8-27b`); mỗi lần chuyển đường ghi một dòng `bot_errors` để thẻ Quota tiêu hao (FR-192) thấy được. Hai giới hạn đã biết: Groq KHÔNG có bộ nhớ tạm prompt nên hai ô cache ghi 0 (thẻ Chi phí Model tụt tỷ lệ đọc lại trong ngày chạy dự phòng là đúng, không phải hỏng); các model Groq đang bật KHÔNG nhìn được ảnh nên lượt phân loại ảnh ném lỗi để đi đúng nhánh "không phân loại được → cất RIÊNG TƯ" của FR-185. Đo 10/09 trên đúng khuôn prompt người bán: qwen3.8-27b trả lời tự nhiên và giữ đúng câu hỏi bắt buộc trong 465 ms; gpt-oss-120b cũng đạt, chậm hơn; gpt-oss-20b trả rỗng, không dùng. Kiểm: TS-GROQ-01…03. | S | chủ dự án 10/09/2026 ~11:10 |
| FR-195 | ✅ **Lượm thông tin DỰ ÁN từ chat, có cửa duyệt.** [nguồn: chủ dự án 10/09/2026 — "thông tin của dự án nó có lượm lặt thêm trong khi chat với khách hàng được ko, lưu ở đâu" → "làm đi"] Trước bản này mọi thứ bot bóc được đều gắn với TIN (`listing_facts`, `listings.boc_tach`, cột `listings`); `projects` chỉ được ghi bởi `scripts/thu-du-an.mjs`, nên chủ nhà kể "toà này phí quản lý 16 nghìn/m2" thì tin sau của người khác trong cùng dự án không hưởng gì. Nay `chat-reply` chép fact sang bảng mới `project_facts` (`20260910f`) khi tin có `project_id` và khoá nằm trong `KHOA_DU_AN` (phí quản lý, phí gửi xe, tiện ích gần, hạ tầng, khu biệt lập, thang máy, PCCC, năm xây, xây dựng, mật độ, tầng cao, sở hữu, thời hạn, điện nước). Mỗi dòng giữ đủ dấu vết: ai nói, từ tin nào, hội thoại nào. **KHÔNG ghi thẳng vào `projects`**: bảng đó là thứ bot đọc để nói với MỌI khách, một người nhớ nhầm là cả kho sai theo và không ai biết sai từ đâu. Dòng vào ở trạng thái `cho_duyet`; admin bấm ở thẻ "Thông tin dự án nghe được trong lúc chat" (tab Vận hành) → `duyet_fact_du_an()` nối tiện ích vào `amenities` hoặc ghi khoá vào `specs` rồi đóng dấu. Không có trigger tự nhập. Kiểm: e2e N34, TS-DUAN-02. | S | chủ dự án 10/09/2026 |
| FR-196 | ✅ **Số về dự án lấy từ KHO, không lấy từ trí nhớ model; model đào lại hồ sơ dự án thành bảng.** [nguồn: chủ dự án 10/09/2026 — "đối chiếu với các cách trả lời xem nó trả lời được chưa, nếu cần thì cho llm đi vào sâu hơn, bóc tách lại nguyên dự án và mô tả luôn"] Đo 5 câu hỏi dự án trên production: bot trả lời trôi chảy nhưng **đè số của kho bằng trí nhớ của model** — kho ghi The Beverly studio 32,5m² và 2PN 78,7m², bot nói "1 phòng ngủ hơn 30m2, 2 phòng ngủ 55-70m2". (a) **Luật số** trong cả hai khối dự án (nhánh mua và nhánh bán): mọi con số về dự án — diện tích từng loại căn, số căn, số tầng, giá, phí, năm bàn giao — chỉ được lấy NGUYÊN VĂN từ khối; không có thì nói "con số đó em xác nhận lại rồi báo", cấm lấy từ trí nhớ. (b) Khối dự án phía NGƯỜI BÁN trước chỉ có 6 tiện ích và 200 ký tự mô tả, không có `specs` — model không có số để mà dùng. Nay có `specs`, 12 tiện ích, 400 ký tự. (c) `scripts/dao-sau-du-an.mjs`: model đọc lại hồ sơ đã thu về và bóc thành `specs.loai_can[]` (tên · diện tích · số căn), `specs.ket_noi[]`, `specs.quy_mo`, `specs.ban_giao`, `amenities` chỉ còn tiện ích NỘI KHU, cộng mô tả 2–3 câu viết lại. Luật của lượt bóc: chỉ dùng đúng văn bản đưa vào, thiếu thì để trống; ô nào model điền mà không có chữ số thì bỏ (nó hay điền tên dự án vào ô bàn giao). Đóng dấu `specs.dao_sau_at` để chạy lại không đào trùng. Trình đào xoay vòng nhiều model vì bậc miễn phí Groq chặn nhịp theo từng model (lượt đầu hụt 32/60, sau khi xoay còn 1/45). Kiểm: TS-DUAN-03. | S | chủ dự án 10/09/2026 |
| FR-197 | ✅ **Dọn dữ liệu KIỂM THỬ lúc 21 giờ mỗi tối; màn hình nói đúng trạng thái model; chuông đếm theo LOẠI lỗi.** [nguồn: chủ dự án 10/09/2026 nhìn màn hình /admin — "model sửa có cả groq rồi mà nhỉ", "cần xử lý 0 mà chuông tbao vẫn 99+", "ko xóa, có script xóa thì chạy trong khoảng 9h tối nhé"] (a) `don_du_lieu_thu()` + cron `don-du-lieu-thu` 14:00 UTC (21:00 giờ VN) xoá người thử theo TIỀN TỐ Zalo ID (`thu-`, `b15-`, `hoi-`, `z-`, `e2e-`) cùng toàn bộ tin, hội thoại, fact và hàng đợi của họ — khách thật mang ID toàn chữ số nên không có đường nào dính. Đóng dấu `bot_health(don_thu)` và ghi số dòng đã xoá vào `app_config.don_thu_lan_cuoi`: một hàm cron không để lại dấu vết là một hàm không kiểm được (NFR-18). (b) Băng cảnh báo: hết số dư mà CÓ đường dự phòng thì viết "Model chính hết số dư — bot đang chạy bằng model dự phòng", không viết "KHÔNG gọi được" nữa; `quota_tieu_hao()` trả thêm `co_du_phong` và `dang_chay_du_phong` (`20260910h`). Băng nói sai trạng thái thì lần sau không ai tin nó nữa. (c) Chuông đếm **số LOẠI lỗi** trong 24 giờ, không đếm từng dòng — một sự cố lặp 80 lần vẫn là một việc phải xử; trước đó chuông đứng "99+" cạnh ô "Cần xử lý" bằng 0, và một con số không nói được gì thì người ta thôi nhìn nó. (d) Lượt XOAY MODEL của lưới dự phòng thôi ghi vào sổ lỗi (chỉ `console.log`) — đó là đường đi đúng thiết kế, ghi vào sổ là tự nuôi còi báo động (bài học `escalation-feed` 08/09). Kiểm: TS-DON-01. | S | chủ dự án 10/09/2026 |
| FR-198 | ✅ **Model chính hạ xuống Haiku 4.5, đổi được bằng secret mà không cần deploy; tham số riêng của họ Claude 5 phải lọc theo model.** [nguồn: chủ dự án 10/09/2026 sau khi soi số dư API — "tao mới check api còn 15 đô", "hạ bot xuống haiku 4.5 đi"] (a) `MODEL` trong `_shared/claude.ts` đọc secret `ANTHROPIC_MODEL`, mặc định `claude-haiku-4-5-20251001` — đổi model từ nay là sửa một dòng trong Vault, không phải mở PR rồi deploy. (b) `output_config.effort` là tham số RIÊNG của họ Claude 5; Haiku 4.5 trả 400 `invalid_request_error: This model does not support the effort parameter` — đo thẳng trên API trước khi đổi. Chỗ này nguy vì IM: `nenDoiSang()` cố ý không coi `invalid_request` là cớ đổi sang Groq (sai tham số thì đổi model không chữa được), nên nếu để lọt thì cả 10 chỗ gọi đều ném và `chat-reply` rơi hết về câu mẫu tiền định — bot vẫn trả lời trơn tru, khách không thấy gì lạ, đúng cảnh sáng 10/09 đẻ ra FR-194. `_shared/tham-so-model.ts` lọc theo tên model ngay trước khi gọi, ở MỘT chỗ, và không đụng `output_config.format` (structured output Haiku 4.5 nhận bình thường — cũng đã đo). (c) Đường dự phòng Groq (FR-194) giữ nguyên: hạ model chính không đụng gì tới lưới. Kiểm: TS-MODEL-01. | S | chủ dự án 10/09/2026 |
| FR-199 | ✅ **Lưới VÉT bằng model: câu có mùi dự án mà tầng tiền định không nhặt được gì thì cho model bóc, kết quả vào hàng chờ duyệt.** [nguồn: chủ dự án 10/09/2026 — "ủa cái nào cũng phải viết hàm như này chứ ko dùng ai tự bóc tách ra specs rồi viết vào dc hả" → "lắp đi"] (a) Được dùng model ở ĐÂY mà không được dùng cho giá / diện tích / phường vì thứ bóc ra KHÔNG vào kho: nó vào `project_facts` trạng thái `cho_duyet` (FR-195), admin gật mới thành sự thật — model bịa thì chết ở hàng chờ, không chết trong rổ hàng. Giá/diện tích ghi thẳng vào `listings`, ở đó một lượt bịa là một tin sai vĩnh viễn. (b) Ba cái van giữ tiền: chỉ chạy khi tầng tiền định KHÔNG ghi được fact dự án nào trong lượt đó; chỉ khi câu có mùi dự án (`coMuiDuAn` — nhắc dự án/toà/phí quản lý/tiện ích…); và tối đa một lượt mỗi tin. Câu thường (giá, “ok em”) không bao giờ đi qua đây. (c) Model bị ép bằng JSON schema và luật “chỉ lấy chữ CÓ TRONG CÂU, không chắc thì trả rỗng”; bộ dọn cắt đuôi địa bàn khỏi tên, bỏ tên quá dài (>6 chữ) hoặc quá ngắn, cắt giá trị còn 120 ký tự, tối đa 6 fact một tin. (d) Dòng do máy đọc ghi `nguon = 'llm'` chứ không phải `seller_chat` — admin duyệt phải phân biệt được lời người nói thẳng với chữ máy suy ra. (e) Phần tiền định (van + bộ dọn) nằm ở `_shared/extraction/vet-du-an-loc.ts` để kiểm được mà không tốn đồng nào; phần gọi model ở `_shared/ai/boc-du-an.ts` và KHÔNG ghi DB (luật ranh giới). Kiểm: TS-VET-01. | M | chủ dự án 10/09/2026 |
| FR-200 | ✅ **Sửa prompt bot ngay trong CRM (`/admin/prompt`), và màn hình nói rõ sửa xong thì đồng bộ tới đâu.** [nguồn: chủ dự án 10/09/2026 — "có chỗ để điều chỉnh prompt bot trong crm, tao chỉ cần sửa chỗ đó thì nó đồng bộ hết luôn chưa"] (a) Trước đó phải mở Supabase Table Editor mới sửa được `bot_prompts`; bảng đó cố ý chỉ mở cho `service_role` nên web không ghi thẳng. Hai hàm SECURITY DEFINER tự kiểm `la_admin()` (`doc_bot_prompts`, `sua_bot_prompt` — `20260910k`) mở đúng một cửa hẹp, mỗi lượt ghi để lại `sua_boi` + `updated_at`. (b) Prompt RỖNG bị chặn ở hàm, không phải ở giao diện: bôi trắng ô rồi lưu là bot mất luật, mà hậu quả chỉ lộ ra trong tin nhắn khách nhận. (c) Màn hình trả lời thẳng câu hỏi “đồng bộ hết chưa”: lưu là **mọi kênh đổi trong 60 giây, không cần deploy** (bảng đè code lúc chạy — FR-138); thứ KHÔNG tự đồng bộ là bản trong git, nên mỗi khoá hiện nhãn *khớp code* / *lệch với code* bằng cách so thẳng với `_shared/prompts.ts` (nhập trực tiếp, không chép tay), kèm nút lấy lại bản trong code. Khoá chỉ có trong code mà kho chưa có cũng được nêu tên. Đây đúng chỗ đẻ ra sự cố 10/09: DB cũ hơn code, bot chạy bộ câu hỏi cũ mấy ngày. Kiểm: TS-PROMPT-01. | S | chủ dự án 10/09/2026 |
| FR-201 | ✅ **Rao tin chỉ đi qua màn SỬA: nút “Duyệt & rao tin” nằm trong đó, dropdown ngoài bảng không rao được nữa.** [nguồn: chủ dự án 10/09/2026 — "nút duyệt cho rao tin thì phải vào 1 tab edit đã chớ mày, nút sửa như này thì đã là sửa xong duyệt luôn chưa"] (a) Trước đó “Lưu thay đổi” giữ nguyên trạng thái đang chọn, còn dropdown trạng thái ngoài bảng cho nhảy thẳng sang *đang bán* — tức là rao được một tin ra web bằng một cú trượt chuột, chưa ai đọc lại nội dung. (b) Nay: trong màn sửa có hai nút tách bạch — *Lưu thay đổi* (không đụng tới việc tin có đang rao hay không) và *Duyệt & rao tin* (lưu rồi đặt `status = dang_ban`), hỏi xác nhận và nêu tên những ô còn thiếu (giá, diện tích, quận, mô tả) trước khi rao. Tin đang rao thì chỗ đó hiện nhãn thay vì nút. (c) Ngoài bảng, mục *đang bán* bị khoá kèm chữ “— qua nút Sửa”. Rao tin là hành động ngoảnh ra ngoài (ai cũng đọc được), nên người bấm phải nhìn thấy đủ tin trước mặt. Kiểm: TS-DUYET-01. | S | chủ dự án 10/09/2026 |
| FR-202 | ✅ **Lời nhắc chủ động đi Zalo clone khi chưa có OA — tới đúng Zalo ID người nhận, và chủ nhà cũng được nhắc trước buổi xem.** [nguồn: chủ dự án 11/09/2026 — "Nhắc lịch xem nhà đánh sent trong khi chưa có OA nên không gửi đi đâu, có zalo clone rồi mà, dùng zalo clone gửi cho id zalo chủ nhà"] (a) Trước: `nudge` soạn xong, không có OA, vẫn đánh `sent` (review 10/09, C1). Bản vá 10/09 để `pending` “cho bridge kéo” — nhưng bridge chỉ kéo `escalation-feed`, cửa đó chỉ phục vụ `escalation`/`report`, nên không ai kéo; còn `nhan_viec_nhac` nhận lại dòng đó sau 5 phút và model soạn lại cùng một tin mãi mãi. (b) Nay tin đã soạn cất vào `reminders.noi_dung_gui` (`20260911b`) và `nhan_viec_nhac` bỏ qua dòng đó; `escalation-feed` trả nó cho bridge kèm Zalo ID của người mua hoặc chủ nhà — người mua KHÔNG BAO GIỜ kèm SĐT (NFR-07), chủ nhà theo SEC-07 (chỉ khi chưa biết uid). Ack xong mới `sent`, và tin vào hội thoại lúc ĐÃ GỬI chứ không phải lúc soạn. (c) Không OA mà người nhận chưa có Zalo ID → báo hỏng lùi dần (FR-166), không đánh `sent`. (d) Lịch xem: `nudge` đặt thêm một dòng cho CHỦ NHÀ của tin, mẫu cố định không gọi model (“có khách hẹn xem căn #… lúc …, anh/chị sắp xếp mở cửa giúp em”) — trước đó chỉ khách được nhắc, trong khi người mở cửa là chủ. Dòng đó không mang `viewing_id` (chỉ mục một-nhắc-mỗi-buổi-xem) và không có `buyer_id` (trigger hỏi cảm nhận FR-56 không chạy); trùng ghi chú thì không đặt lần hai. Kiểm: TS-NHAC-CLONE-01. | S | chủ dự án 11/09/2026 |
| FR-203 | ✅ **Đăng nhập lại acc Zalo clone bằng mã QR hiện ngay trên `/admin`; bridge tự nhận ra phiên chết.** [nguồn: chủ dự án 11/09/2026 — "cần quét zalo clone lại ko, viết lên crm dc ko", "qr cũng hiện trên crm để quét là được"] (a) Trước: cookie `zpw_sek` hết hiệu lực thì bridge không biết — cứ kéo việc, gửi hỏng, ghi lỗi mỗi phút (chiều 10/09 → sáng 11/09, 21 tin báo admin nằm chờ); muốn quét lại phải ssh vào VPS đọc `journalctl` lấy link QR. (b) Nay bridge thấy lỗi `zpw_sek` (lúc khởi động, lúc gửi tin, lúc kéo việc) → cất phiên sang `zalo-session.het-han.json`, thoát, systemd dựng lại vào luồng QR; ảnh QR đi lên bảng `bridge_dang_nhap` qua `escalation-feed` (action `qr`, cùng cổng bí mật của bridge). (c) `/admin` có ô *Zalo clone*: trạng thái, mã QR (đọc lại mỗi 4 giây; mã quá 110 giây thì không hiện), nút *Đăng nhập lại* (hàm `yeu_cau_quet_lai_zalo()` tự kiểm `la_admin()`; bridge nhận ở lượt kéo việc kế tiếp, ≤ 5 phút). (d) zca-js 2.x KHÔNG tự sinh mã mới khi QR hết hạn nếu có callback — bản trước đứng im sau 100 giây; nay gọi `actions.retry()` — SAU khi lệnh báo *hết hạn* đã tới DB, và `escalation-feed` xếp các lệnh theo giờ sự kiện ở bridge (`luc`) chứ không theo giờ request tới, lệnh cũ hơn trạng thái đang lưu thì bỏ. Lý do: 11/09 09:27 lệnh *hết hạn* gửi trước mà tới sau, xoá mất ảnh của mã mới, `/admin` trống QR suốt 100 giây. (e) Ảnh QR nhạy — quét bằng acc nào thì acc đó thành acc bot — nên chỉ admin đọc (RLS), không mở anon, và bị xoá ngay khi rời trạng thái `cho_quet`. Link http tạm trên VPS và `qr.png` vẫn giữ làm đường dự phòng. (f) [nguồn: chủ dự án 11/09/2026 chụp `/admin` — "trợ lý bot đang trực đâu khi mà zalo đang lỗi, nếu bot lỗi thì hiện tab"] Thẻ *Trợ lý AI Bot* trước chỉ nhìn nhịp tim bridge, mà bridge chờ quét QR vẫn gõ cửa đều (mỗi lần đẩy mã là một nhịp) → thẻ báo *Đang trực* xanh đúng lúc Zalo chết. Nay thẻ báo **Đang lỗi** kèm lý do khi bridge im quá 15 phút, khi Zalo clone chưa đăng nhập, hoặc khi model hết số dư mà không có dự phòng; Zalo chưa đăng nhập thì ô mã QR lên **đầu trang ở mọi tab**, và tab *Vận hành* trên thanh trên có chấm đỏ. Kiểm: TS-QR-01. | S | chủ dự án 11/09/2026 |
| FR-187 | ✅ **JSON bóc tách CHIA NHÓM, mỗi mã tin một file; trường trống không hiện; xem ngay trong CRM.** (10/09: ảnh chủ gửi được model ĐỌC trước khi cất kho — `AnhSchema.khen` một điểm mạnh thật thấy trong ảnh → bot khen bằng điểm đó, không khen suông — FR-185 bổ sung; "Còn căn nữa: 7 Hồng Bàng…" từ người bán quen tạo TIN THỨ HAI, không còn bị hiểu là sửa tin cũ — e2e N21.) [nguồn: chủ dự án 09/09/2026 tối ("chia luôn từ lúc có câu rao phân loại… file json dành cho từng mã BĐS chứ không phải nguyên một rổ dài… trang null trong Supabase thì không cần hiển thị… chia nhóm luôn, đọc lại 2 cái chat Gemini xem có trường hợp nào, đây là production nên không thể bỏ sót"); chat Gemini 21/06 lượt 38 (§III SRD: toà nhà dòng tiền, đất nông nghiệp, SKC/TMD, kho xưởng) + lượt 65–67 (câu hỏi bù: móng, hẻm thông, ngập, cách mặt tiền, trường học, công chứng, gym); chat 07/09 (sổ cầm tay hay thế chấp, đang ở hay cho thuê, động cơ bán, giá linh động)] Trước: `listings.boc_tach` là túi phẳng mọi khoá (trigger `trg_zz_fact_vao_boc_tach`), trang `/admin/ro-hang/json` đọc thẳng cột + tải CẢ RỔ thành một file. Nay: hàm DB `boc_tach_nhom(listings) → jsonb` gộp cột `listings` + fact mới nhất mỗi khoá (`listing_facts`) + ảnh trong kho (`listing_media`: loại, kho, đường dẫn, nguồn, mô tả, OCR) + `boc_tach` + `diem_tin` thành **12 nhóm**: `tin` (mã, loại giao dịch, loại BĐS, trạng thái, gấp, nguồn, tự chốt, mốc thời gian) · `nguoi_rao` (tên, vai, trợ lý) · `vi_tri` (địa chỉ, đường, phường, quận, lời chủ nói, dự án, mã căn, loại đường, hẻm m, cách mặt tiền, hẻm thông, ngập, hạ tầng, tiện ích gần, compound, container, toạ độ) · `thong_so` (diện tích các loại, ngang/dài/nở hậu, thổ cư, hình dáng, tầng/kết cấu, phòng ngủ, WC, hướng, view, căn góc, thang máy, ô tô, sân vườn, hiện trạng, năm xây, mật độ, tầng cao, số phòng, PCCC, chiều cao, tải trọng, biến áp, nước thải, đường vào, nguồn nước, ranh giới) · `phap_ly` (tình trạng, hoàn công, quy hoạch, lời chủ, thế chấp, xây dựng, sở hữu, lên thổ cư, thời hạn, hình thức thuê đất, mục đích) · `gia` (raw, VNĐ, VNĐ/m², thương lượng, gấp, lý do bán, đang ở/thuê) · `cho_thue` (cọc, thời hạn, trượt giá, fit-out, nội thất, phí QL, phí xe, điện nước, giờ giấc, doanh thu, lấp đầy) · `khai_thac` (tiềm năng, ngành hàng) · `anh` (số tấm, kho[], URL chat[]) · `bo_sung[]` (câu chủ nhà nói thêm, theo thứ tự) · `cham_soc` (chấm điểm, kết thúc, mốc bóc tách) · `diem` (tổng, 8 mục, còn thiếu). `jsonb_bo_rong()` bỏ null / chuỗi rỗng / object-array rỗng đệ quy — "trường null trong Supabase thì không cần hiển thị". Nhóm nào chưa có gì thì không xuất hiện — đó là cách thấy tin còn thiếu gì. Đọc qua view `boc_tach_v` (security_invoker, RLS `listings_admin_read`; revoke anon) và RPC `boc_tach_nhom(uuid)` (kiểm `la_admin()`); sổ `so.boc_tach` để mở trong Table Editor. Web: `components/admin/BocTachNhom.tsx` — 12 ô nhóm, đếm trường, nút **Chép JSON** và **Tải <mã tin>.json** (TỪNG TIN, bỏ nút tải cả rổ); `/admin/ro-hang/json` đọc `boc_tach_v`, lọc theo loại BĐS và theo "có nhóm X", tìm trong JSON, hiện điểm; `/admin/ro-hang` thêm nút **JSON** mỗi dòng mở hàng con ngay dưới tin (đọc một tin khi bấm, không tải cả rổ). Không đụng `boc_tach` cột gốc (trigger, `boc_thong_so` vẫn dùng). | M | chủ dự án 09/09/2026 tối ("chia luôn từ lúc có câu rao phân loại… file json dành cho từng mã BĐS chứ không phải nguyên một rổ dài… trang null trong Supabase thì không cần hiển thị… chia nhóm luôn, đọc lại 2 cái chat Gemini xem có trường hợp nào, đây là production nên không thể bỏ sót"); chat Gemini 21/06 lượt 38 (§III SRD: toà nhà dòng tiền, đất nông nghiệp, SKC/TMD, kho xưởng) + lượt 65–67 (câu hỏi bù: móng, hẻm thông, ngập, cách mặt tiền, trường học, công chứng, gym); chat 07/09 (sổ cầm tay hay thế chấp, đang ở hay cho thuê, động cơ bán, giá linh động) |

---

## 2.5 Yêu cầu phi chức năng

| ID | Loại | Yêu cầu | Đo bằng |
|---|---|---|---|
| NFR-01 | Hiệu năng | 🟡 Bot phản hồi tin nhắn thường < 3s (p95). Hiện thực: view `bot_do_tre` (p50/p95 7 ngày) trên `/admin`. Chưa: chưa chốt số đo trên lưu lượng thật. | Log; thẻ `/admin` |
| NFR-02 | Hiệu năng | 🟡 Trang listing/search LCP < 2.5s trên 4G. Hiện thực: ISR + chọn cột + ảnh lazy (FR-171 j). Chưa: chưa chạy Lighthouse. | Lighthouse ≥ 90 mobile |
| NFR-03 | Sẵn sàng | 🟡 Zalo OA phục vụ 24/7, uptime ≥ 99.5%. Hiện thực: nhịp tim bridge + sổ lỗi (FR-152). Chưa: chưa có công cụ đo uptime. | Monitoring |
| NFR-04 | Tin cậy | ✅ Tin nhắn **không được mất** khi một bên offline → hàng đợi bền. Hiện thực: sổ inbound + đường cứu mỗi phút (FR-162/166). | PDF hệ thống §5 |
| NFR-05 | Quy mô | 🟡 6 tháng đầu: ~10 chat mới/ngày, ~300 chat sống đồng thời, ~5.000 listing. Chưa: chưa chạm ngưỡng nên chưa đo tải thật. | BR-03, BR-01 |
| NFR-06 | Bảo mật | ✅ Ảnh sổ đỏ/giấy tờ lưu bucket riêng, admin xem bằng link ký hạn 15 phút; đường dẫn riêng không bao giờ ra trang công khai. Hiện thực: bucket `listing-private` + thẻ giấy tờ ở `/admin`. | RSK-07 |
| NFR-07 | Riêng tư | ✅ Không thu số ĐT của B ngoài bước đặt lịch xem nhà; cho phép ngắt kết nối bất cứ lúc nào. Hiện thực: `/admin` không đọc cột `phone`, câu cam kết trên web. | BR-06, FR-04 |
| NFR-08 | Riêng tư | ❌ Fingerprint chỉ dùng cá nhân hoá kết quả, có thông báo và cơ chế từ chối. Chưa: đi cùng FR-16 (OPEN-14). | FR-16 |
| NFR-09 | SEO | 🟡 100 URL tag render server-side, có canonical, sitemap, structured data. Hiện thực: 64 tag SSG + JSON-LD (FR-12/17). Chưa: bộ TOP-100 keyword (OPEN-06). | BR-08 |
| NFR-10 | i18n | ✅ Toàn bộ giao diện và nội dung chat: tiếng Việt, xưng hô anh/chị/em. | chats w B.docx |
| NFR-11 | Vận hành | 🟡 Mọi dữ liệu thống kê phải **kết nối được từ Excel** — không cần dashboard đẹp. Hiện thực: view + CSV ở `/admin`, đọc thẳng qua Table Editor. Chưa: xuất Excel phía server. | chats w B.docx §Thống kê |
| NFR-12 | Khả chuyển | 🟡 Tầng messaging trừu tượng hoá để thêm Messenger/Telegram mà không sửa lõi. Hiện thực: bộ não tách khỏi kênh (OA và bridge cùng gọi một cửa). Chưa: chưa có adapter kênh thứ ba. | RSK-02 |
| NFR-13 | Khả kiểm | ✅ Mọi sự kiện listing và hội thoại có timestamp, truy vết được. Hiện thực: `property_events`, `messages.seq`, sổ lỗi. | FR-70, FR-71 |
| NFR-14 | Chi phí | 🟡 Tổng chi phí build MVP ≤ 418tr VND. Hiện thực: hạ tầng còn ở bậc miễn phí (NFR-16). Chưa: chưa có số đo chi phí thực tế. | §1.4 |
| NFR-15 | Tuân thủ | ✅ Asset theme thương mại chỉ dùng trong phạm vi license đã mua (regular = 1 end product); không commit theme vào repo public. | OPEN-07 chốt 24/08/2026 |
| NFR-16 | Chi phí | ✅ **Free-tier trước, trả tiền sau**: mọi dịch vụ hạ tầng khởi đầu ở bậc miễn phí, chỉ nâng cấp khi chạm ngưỡng đo được và ghi lại ngưỡng trong docs. Ràng buộc kèm theo: bậc Free không có backup tự động (OPEN-25) — 11/09/2026 chủ dự án bỏ luôn sao lưu tự làm, chấp nhận rủi ro mất dữ liệu. | trao đổi chủ dự án 24/08/2026 |
| NFR-17 | Hiệu năng | ✅ **Trang tin phải nằm trong cache**, không hỏi DB từng lượt xem. Route động có tham số đường dẫn bắt buộc khai `generateStaticParams()`; route đọc `searchParams` thì bọc truy vấn trong Data Cache. Nghiệm thu: bảng route sau build hiện `●`/`○` cho trang tin. | soát cloud/compute 27/08/2026 |
| NFR-18 | Vận hành | ✅ **Xanh phải là xanh thật**: không dùng trạng thái của lớp xếp hàng thay cho kết quả thật của việc — `cron.job_run_details.status` không phải bằng chứng bot chạy được. Kết quả thật phải được quét và ghi vào sổ bền (FR-152). | soát cloud/compute 27/08/2026 |

## 2.6 Giả định

| ID | Giả định | Đổ vỡ nếu sai |
|---|---|---|
| ASM-01 | Zalo OA cho phép gửi tin chủ động ở tần suất cần cho FR-63, FR-64 | Toàn bộ chiến lược giữ chân sụp |
| ASM-02 | Zalo SSO khả dụng cho ứng dụng bên thứ ba của nhadat.cc | Phải làm OTP/đăng nhập riêng |
| ASM-03 | AI bóc tách địa chỉ Việt Nam (hẻm, ngã tư, mốc tiện ích) đạt độ chính xác dùng được | FR-92, FR-22 phải có người kiểm duyệt |
| ASM-04 | CCRB/NMG chịu trả lời câu hỏi bổ sung trong vài giờ | Vòng lặp INS-06 đứt, B mất kiên nhẫn |
| ASM-05 | Giao dịch được ghi nhận và thu phí ngoài hệ thống (thủ công, hợp đồng giấy) | Cần module hợp đồng/thanh toán, vượt ngân sách |
